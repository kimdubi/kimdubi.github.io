<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Postgresqls on kimDuBiA</title>
    <link>https://kimdubi.github.io/postgresql/</link>
    <description>Recent content in Postgresqls on kimDuBiA</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <lastBuildDate>Sun, 20 Sep 2020 21:45:34 +0900</lastBuildDate>
    
        <atom:link href="https://kimdubi.github.io/postgresql/index.xml" rel="self" type="application/rss+xml" />
    
    
    <item>
      <title>PostgreSQL HOT update</title>
      <link>https://kimdubi.github.io/postgresql/psql_hot/</link>
      <pubDate>Sun, 20 Sep 2020 21:45:34 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_hot/</guid>
      <description>&lt;p&gt;PostgreSQL의 MVCC 는 ORACLE이나 MySQL의 동작 방식과 다릅니다.&lt;/p&gt;
&lt;p&gt;과거 데이터를 별도의 UNDO 영역에 저장하는 게 아니라 과거의 tuple을 invalid 처리 후 update 후의 새로운 tuple 을 추가하는 방식으로 동작하는데요&lt;br&gt;
&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_hot_01.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;장점으로는&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;UNDO 영역을 따로 관리할 필요가 없다&lt;/li&gt;
&lt;li&gt;ROLLBACK 처리기 매우 빠르다&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;단점으로는&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;invalid 된 오래된 dead tuple들을 정리할 vacuum 작업이 필요하다&lt;/li&gt;
&lt;li&gt;과도한 업데이트로 인한 dead tuple 로 테이블이 비대해 질 수 있다&lt;/li&gt;
&lt;li&gt;update 로 인한 인덱스 수정이 필요하다&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;같은 장단점이 있습니다.&lt;br&gt;
그리고 이를 어느정도 해소하기 위한 방법이 HOT (Heap Only Tuple) 업데이트 방식인데요&lt;br&gt;
&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_hot_02.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;그림처럼 테이블 block 외부에서는 어디서도 참조되지 않는 HOT tuple 을 생성합니다.&lt;br&gt;
그리고 이 HOT tuple 의 위치는 old version의 row가 갖고 있는데 마치 pointer 처럼 old version의 row, HOT chain을 따라&lt;br&gt;
새로 업데이트된 HOT tuple 에 도착할 수 있습니다.&lt;/p&gt;
&lt;p&gt;이 방법에는 다음과 같은 장점이 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;인덱스를 수정할 필요가 없다
=&amp;gt; tuple 의 외부 주소는 변한 게 없고 index 는 tuple의 HOT chain 을 따라가면되니까&lt;/li&gt;
&lt;li&gt;dead tuple에 대한 vacuum 부담이 적어진다.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;이 HOT tuple을 이용한 update를 이용하려면 두가지 조건을 충족해야하는데&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;block 에 공간이 충분해야한다
=&amp;gt; old version의 tuple과 HOT tuple 이 같은 block 에 있어야함, 아래 그림의 (a)&lt;/li&gt;
&lt;li&gt;index key 값에 변경이 없어야 한다
=&amp;gt; index key 값이 update 되어 변경이 생기면 새로운 인덱스 tuple이 생성됨,아래 그림의 (b)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_hot_03.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;h2 id=&#34;heading&#34;&gt;테스트&lt;/h2&gt;
&lt;h3 id=&#34;hot-tuple--&#34;&gt;HOT tuple 미사용 시&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;데이터 생성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;CREATE TABLE mytable (
   id  integer PRIMARY KEY,
   val integer NOT NULL
) WITH (autovacuum_enabled = off);
 
INSERT INTO mytable
SELECT *, 0
FROM generate_series(1, 235) AS n;

SELECT ctid, id, val FROM mytable;

  ctid   | id  | val 
---------+-----+-----
 (0,1)   |   1 |   0
 (0,2)   |   2 |   0
 (0,3)   |   3 |   0
 (0,4)   |   4 |   0
 (0,5)   |   5 |   0
...
 (0,224) | 224 |   0
 (0,225) | 225 |   0
 (0,226) | 226 |   0
 (1,1)   | 227 |   0
 (1,2)   | 228 |   0
 (1,3)   | 229 |   0
 (1,4)   | 230 |   0
 (1,5)   | 231 |   0
 (1,6)   | 232 |   0
 (1,7)   | 233 |   0
 (1,8)   | 234 |   0
 (1,9)   | 235 |   0
(235 rows)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; id 1~226 까지 block 0, 그 이후의 데이터는 block 1 에 저장됨 확인&lt;br&gt;
ctid 는 current tuple ID 로 oracle 의 rowid 와 비슷한 개념&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;update 수행 후 CTID&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;UPDATE mytable
SET val = -1
WHERE id = 42;
 
SELECT ctid, id, val
FROM mytable
WHERE id = 42;
 
  ctid  | id | val 
--------+----+-----
 (1,10) | 42 |  -1
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; update 된 id = 42 가 신규 block 으로 ctid (1,10) 에 생성됨,&lt;br&gt;
old verseion 의 tuple 과 block이 달라졌기 때문에 새로운 ctid를 가리키는 index tuple이 새로 생성되어야 함&lt;/p&gt;
&lt;h3 id=&#34;hot-tuple--1&#34;&gt;HOT tuple 사용 시&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;TRUNCATE mytable;
 
ALTER TABLE mytable SET (fillfactor = 70);
 
INSERT INTO mytable
SELECT *, 0 FROM generate_series(1, 235);
 
SELECT ctid, id, val
FROM mytable;
 
  ctid   | id  | val 
---------+-----+-----
 (0,1)   |   1 |   0
 (0,2)   |   2 |   0
 (0,3)   |   3 |   0
 (0,4)   |   4 |   0
 (0,5)   |   5 |   0
...
 (0,156) | 156 |   0
 (0,157) | 157 |   0
 (0,158) | 158 |   0
 (1,1)   | 159 |   0
 (1,2)   | 160 |   0
 (1,3)   | 161 |   0
...
 (1,75)  | 233 |   0
 (1,76)  | 234 |   0
 (1,77)  | 235 |   0
(235 rows)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;update 후 CTID&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;UPDATE mytable
SET val = -1
WHERE id = 42;
 
SELECT ctid, id, val
FROM mytable
WHERE id = 42;
 
  ctid   | id | val 
---------+----+-----
 (0,159) | 42 |  -1
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; fillfactor 는 ORACLE의 PCTFREE의 개념으로 block을 어느정도 사용할지 정하는 설정,&lt;br&gt;
위에서는 테이블의 fillfactor를 70%로 설정하여 block의 70%만 사용하고 30%는 update를 위해 남겨두게 된다&lt;br&gt;
그래서 update 수행 후 새로운 tuple이 새로운 block에 생성되는 게 아니라 old version의 tuple 과 같은 block 에 생성되어 인덱스 변경이 필요없음&lt;br&gt;
old version의 tuple 이 update 된 새로운 tuple의 위치를 알려주는 HOT chain 방식을 이용하게된다&lt;/p&gt;
&lt;h2 id=&#34;heading1&#34;&gt;정리&lt;/h2&gt;
&lt;p&gt;postgresql 의 MVCC 는 UNDO 없이 old version, new version 모두 갖고 있는 방식으로 update 할 때 비효율이 있는데&lt;br&gt;
이를 fillfactor 를 이용한 HOT 방식으로 어느정도 해소 할 수 있습니다. 참고로 fillfactor는 전역변수가 아니라 테이블 개별 설정이기 때문에&lt;br&gt;
update 가 잦은 테이블은 fillfactor를 조절하여 HOT update 의 효과를 볼 수 있습니다.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL synchronus_commit</title>
      <link>https://kimdubi.github.io/postgresql/psql_sync/</link>
      <pubDate>Sun, 30 Aug 2020 18:37:48 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_sync/</guid>
      <description>&lt;p&gt;replication 구성에서 중요하게 생각할 부분 중 하나로 sync 방식이 있습니다. MySQL의 경우 sync,async,semi sync 방식으로 설정할 수 있는데요.&lt;br&gt;
이번 글에서는 PostgreSQL의 sync replication 설정과 주의사항에 대해서 알아보겠습니다.&lt;/p&gt;
&lt;h2 id=&#34;wal---ack&#34;&gt;WAL 전달에 따른 ACK&lt;/h2&gt;
&lt;!-- raw HTML omitted --&gt;
&lt;p&gt;(1) 백엔드 프로세스는 XLogInsert () 및 XLogFlush () 함수를 실행하여 WAL 세그먼트 파일에 WAL 데이터를 쓰고 플러시 합니다.&lt;br&gt;
(2) Primary의 walsender 프로세스는 WAL 세그먼트에 기록 된 WAL 데이터를 Standby의 walreceiver 프로세스로 보냅니다.&lt;br&gt;
(3) WAL 데이터를 보낸 후 백엔드 프로세스는 Standby의 ACK 응답을 계속 대기합니다. 보다 정확하게는 백엔드 프로세스는 내부 함수 SyncRepWaitForLSN () 을 실행하여 래치를 얻고 해제 될 때까지 기다립니다.&lt;br&gt;
(4) Standby의 walreceiver는 write () 시스템 호출을 사용하여 받은 WAL 데이터를 WAL 세그먼트에 쓰고 ACK 응답을 Primary의 walsender에게 반환합니다.&lt;br&gt;
(5) Standby의 walreceiver는 fsync () 와 같은 시스템 호출을 사용하여 WAL 데이터를 WAL 세그먼트로 플러시하고 Primary의 walsender에게 또 다른 ACK 응답을 반환 후 업데이트 된 WAL 데이터에 대해 startup 프로세스에 알립니다.&lt;br&gt;
(6) startup 프로세스는 WAL 세그먼트에 기록 된 WAL 데이터를 재생합니다.&lt;br&gt;
(7) Primary의 walsender는 Standby의 walreceiver로부터 ACK 응답을 받으면 백엔드 프로세스의 latch를 해제하면서 백엔드 프로세스의 커밋 또는 중단 작업이 완료됩니다.&lt;/p&gt;
&lt;h2 id=&#34;synchronouscommit-&#34;&gt;synchronous_commit 설정&lt;/h2&gt;
&lt;p&gt;위 단계 중 (7)에서 latch를 해제하는 타이밍은 synchronous_commit 설정에 따라 다릅니다.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_sync3.png&#34; alt=&#34;&#34;&gt; &lt;br&gt;
(예시 : synchronous_commit=on)&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;off : Primary에서 커밋 수행 시 바로 client에게 commit 완료했다고 보냄. 실제 WAL record가 WAL segment에 쓰여지기 까지는 딜레이가 존재하여 서버 crash 발생 시 트랜잭션 손실 가능 ( 최대 wal_writer_delay(200ms)의 세배 )&lt;/li&gt;
&lt;li&gt;local : WAL record가 Primary의 WAL segment 까지 write된 것을 보장, asynchronus replication&lt;/li&gt;
&lt;li&gt;remote_write : Standby가 WAL record를 받아서 OS buffer까지 write 한 것을 보장하는 설정, ( disk에 write는 보장하지 않음) 위 그림에서는 (4) 단계에 해당하는 설정으로 Semi-synchronus replication 개념임&lt;/li&gt;
&lt;li&gt;on : WAL record가 Standby의 WAL segment (disk) 까지 write 되는 것을 보장, 위 그림에서는 (5) 단계에 해당하는 설정으로 Semi-synchronus replication 개념임&lt;/li&gt;
&lt;li&gt;remote_apply : Standby 에서 WAL을 replay 까지 완료하는 것을 보장, 가장 강한 synchronus replication 개념&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;synchronouscommit---&#34;&gt;synchronous_commit 설정 시 주의사항&lt;/h2&gt;
&lt;p&gt;데이터의 안정성을 위해 synchronous_commit = remote_write 이상으로 설정 시엔 주의사항이 있습니다.&lt;br&gt;
바로 Standby 서버에 문제가 생겨 Primary의 walsender에게 ACK 를 주지 못하면 Primary의 트랜잭션도 완료가 안되는 것인데요&lt;br&gt;
이런 경우 MySQL 처럼 semi sync -&amp;gt; async 로 자동으로 변경하는 설정이 없어 조치가 없다면 Primary 에선 트랜잭션을 수행할 수 없는 상태가 지속됩니다.&lt;br&gt;
이를 위해 두가지 방법이 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;수동으로 asynchronus replication 전환&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# SELECT application_name AS host,client_addr
         sync_priority, sync_state FROM pg_stat_replication;
-[ RECORD 1 ]-+-------------
host          | standby1
sync_priority | 10.161.78.43
sync_state    | sync

postgres=# alter system set synchronous_standby_names = &#39;&#39; ;

postgres=# select pg_reload_conf();

postgres=# SELECT application_name AS host,client_addr
         sync_priority, sync_state FROM pg_stat_replication;
-[ RECORD 1 ]-+-------------
host          | standby1
sync_priority | 10.161.78.43
sync_state    | async

&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;Standby 서버를 여러대 붙이기&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_sync1.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;=&amp;gt; Primary 에 여러대의 replication 서버가 있을 때 Primary는 sync_priority 가 높은 (숫자가 1에 가까울수록, 0은 async) standby 서버의 ACK를 최우선으로 합니다.&lt;br&gt;
위 그림에서는 sync_priority=2 인 standby2 의 ACK가 Priamry 로 먼저 도착했지만 Primary는 sync_priority=1 인 standby1 의 ACK를 기다린 후 트랜잭션을 완료했습니다.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_sync2.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;=&amp;gt; 만약 sync_priority=1 인 standby1 이 wal_sender_timeout (60s) 만큼 기다린 후 응답이 없으면 이상을 감지하고 standby2 를 sync 방식의 standby 로 승격 시킨 후 운영하게 됩니다.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL service 등록하기</title>
      <link>https://kimdubi.github.io/postgresql/psql_service/</link>
      <pubDate>Sat, 29 Aug 2020 13:19:43 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_service/</guid>
      <description>&lt;p&gt;yum 설치가 아닌 source 설치를 했을 때는 postgresql 기동 관련 service 설정을 따로해줘야합니다.&lt;br&gt;
이번 글에서는 centos6 / 7 에서 각각 service 등록하는 방법을 알아보겠습니다.&lt;br&gt;
설치한 path는 환경에 맞게 수정하시면 됩니다.&lt;/p&gt;
&lt;h2 id=&#34;postgres-service---centos6&#34;&gt;postgres service 등록 - centos6&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;irteamsu) sudo vi /etc/init.d/postgres

#!/bin/bash
# chkconfig:2345 90 20

# Installation prefix

prefix=/home1/irteam/psql/engn/postgresql-11.7

# Data directory
PGDATA=&amp;quot;/home1/irteam/psql/engn/PGSQL&amp;quot;

# Who to run the postmaster as, usually &amp;quot;postgres&amp;quot;. (NOT &amp;quot;root&amp;quot;)
PGUSER=irteam

# Where to keep a log file
PGLOG=&amp;quot;/home1/irteam/psql/logs/testdb/error_log/alert_testdb.log&amp;quot;

# Check for echo -n vs echo \c
if echo &#39;\c&#39; | grep -s c &amp;gt;/dev/null 2&amp;gt;&amp;amp;1 ; then
        ECHO_N=&amp;quot;echo -n&amp;quot;
        ECHO_C=&amp;quot;&amp;quot;
else
        ECHO_N=&amp;quot;echo&amp;quot;
        ECHO_C=&#39;\c&#39;
fi

# The path that is to be used for the script
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin:/home1/irteam/psql/engn/postgresql-11.7/bin/

# What to use to start up the postmaster (we do NOT use pg_ctl for this,
# as it adds no value and can cause the postmaster to misrecognize a stale
# lock file)

DAEMON=&amp;quot;$prefix/bin/postmaster&amp;quot;

# What to use to shut down the postmaster
PGCTL=&amp;quot;$prefix/bin/pg_ctl&amp;quot;

set -e

# Only start if we can find the postmaster.
test -x $DAEMON || exit 0

# Parse command line parameters.
case $1 in
        start)
                $ECHO_N &amp;quot;Starting PostgreSQL: &amp;quot;$ECHO_C
                su - $PGUSER -c &amp;quot;$DAEMON -i -D $PGDATA&amp;quot; &amp;gt;&amp;gt;$PGLOG 2&amp;gt;&amp;amp;1 &amp;amp;
                echo &amp;quot;ok&amp;quot;
                ;;
        stop)
                echo -n &amp;quot;Stopping PostgreSQL: &amp;quot;
                su - $PGUSER -c &amp;quot;$PGCTL stop -D &#39;$PGDATA&#39; -s -m fast&amp;quot;
                echo &amp;quot;ok&amp;quot;
                ;;
        restart)
                echo -n &amp;quot;Restarting PostgreSQL: &amp;quot;
                su - $PGUSER -c &amp;quot;$PGCTL stop -D &#39;$PGDATA&#39; -s -m fast -w&amp;quot;
                su - $PGUSER -c &amp;quot;$DAEMON -D $PGDATA&amp;quot; &amp;gt;&amp;gt;$PGLOG 2&amp;gt;&amp;amp;1 &amp;amp;
                echo &amp;quot;ok&amp;quot;
                ;;
        reload)
                echo -n &amp;quot;Reload PostgreSQL: &amp;quot;
                su - $PGUSER -c &amp;quot;$PGCTL reload -D &#39;$PGDATA&#39; -s&amp;quot;
                echo &amp;quot;ok&amp;quot;
                ;;
        status)
                su - $PGUSER -c &amp;quot;$PGCTL status -D &#39;$PGDATA&#39;&amp;quot;
                ;;
        *)
                # Print help
                echo &amp;quot;Usage: $0 {start|stop|restart|reload|status}&amp;quot; 1&amp;gt;&amp;amp;2
                exit 1
                ;;
esac
exit $?
&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;postgresql-systemctl---centos7&#34;&gt;postgresql systemctl 등록 - centos7&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;irteamsu) sudo vi /usr/lib/systemd/system/postgresql.service

[Unit]
Description=PostgreSQL 9.6.19
After=syslog.target
After=network.target

[Service]
Type=forking

User=irteam
Group=irteam

# Note: avoid inserting whitespace in these Environment= lines, or you may
# break postgresql-setup.

# Location of database directory
Environment=PGDATA=/home1/irteam/psql/engn/PGSQL
Environment=POSTGRES_HOME=/home1/irteam/psql/engn/postgresql-9.6.19

# Where to send early-startup messages from the server (before the logging
# options of postgresql.conf take effect)
# This is normally controlled by the global default set by systemd
# StandardOutput=syslog

# Disable OOM kill on the postmaster
OOMScoreAdjust=-1000

ExecStart=/home1/irteam/psql/engn/postgresql-9.6.19/bin/pg_ctl start -D &amp;quot;${PGDATA}&amp;quot; -s -w -t 300
ExecStop=/home1/irteam/psql/engn/postgresql-9.6.19/bin/pg_ctl stop -D &amp;quot;${PGDATA}&amp;quot; -s -m fast
ExecReload=/home1/irteam/psql/engn/postgresql-9.6.19/bin/pg_ctl reload -D &amp;quot;${PGDATA}&amp;quot; -s

# Give a reasonable amount of time for the server to start up/shut down
TimeoutSec=300

[Install]
WantedBy=multi-user.target



### systemctl enable

$ sudo systemctl enable postgresql.service
$ sudo systemctl start postgresql.service
$ sudo systemctl stop postgresql.service

&lt;/code&gt;&lt;/pre&gt;</description>
    </item>
    
    <item>
      <title>PostgreSQL replication을 위한 WAL 전달과정</title>
      <link>https://kimdubi.github.io/postgresql/pg_repl-1/</link>
      <pubDate>Tue, 25 Aug 2020 22:38:57 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/pg_repl-1/</guid>
      <description>&lt;p&gt;ORACLE의 RAC 처럼 공유디스크를 사용하지 않는 이상 DBMS는 Master - Slave 구성을 위해 어떤 방식으로든 Master에서 수행한 트랜잭션을 Slave로 전파해야 합니다.&lt;br&gt;
MySQL은 SQL 커맨드를 전파하는 방식으로 하는 반면, PostgreSQL은 트랜잭션 로그를 전달하는 방식으로 replication을 수행합니다.&lt;br&gt;
이 두개의 방식은 비슷한듯, 아래와 같이 다르게 조금은 동작합니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;MySQL&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/mysql_repl.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;PostgreSQL&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/postgre_repl.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;왜 이런 차이가 발생하는 건지 이번 글에서는 PostgreSQL의 Streaming Replication 기준으로 복제 방식을 살펴보겠습니다.&lt;br&gt;
MySQL의 replication 방식은 아래 글을 참고해주세요&lt;br&gt;
&lt;a href=&#34;https://kimdubi.github.io/mysql/semi_sync/&#34;&gt;https://kimdubi.github.io/mysql/semi_sync/&lt;/a&gt;&lt;/p&gt;
&lt;h2 id=&#34;postgresql-wal--&#34;&gt;PostgreSQL WAL 전파 과정&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/postgre_repl2.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;위 그림은 Primary(local) 에서 standby (remote) 로 WAL을 전달하는 과정을 나타내는데 총 5가지 단계가 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;WAL inserts : WAL record는 wal_buffer 에 먼저 생성 되고 wal_buffer 로의 WAL record 생성은 여러 백그라운드 프로세스에 의해 지속적으로 기록됩니다.&lt;/li&gt;
&lt;li&gt;WAL writes &amp;amp; WAL flush : transaction commit / rollback , wal_buffer 사용량 이나 walwriter process 에 의해 WAL logfile로 flush 됩니다.(default 200ms 마다)&lt;/li&gt;
&lt;li&gt;Remote Write : 위 세가지 상황에서 wal_sender 는 WAL record를 remote 의 wal_receiver에게 전달합니다.&lt;/li&gt;
&lt;li&gt;Remote flush : 전달받은 wal record 가 standby 의 디스크에 flush 됩니다.&lt;/li&gt;
&lt;li&gt;Remote apply : 위 wal record를 standby 서버에 적용하여 데이터를 반영합니다.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;streaming-replication-&#34;&gt;Streaming replication 테스트&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;primary 서버&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### auto commit off

postgres=# \set AUTOCOMMIT off
postgres=# \echo :AUTOCOMMIT
off

### DML 전 lsn 

postgres=# select pg_current_wal_lsn();
 pg_current_wal_lsn
--------------------
 7/2D0219E0
(1 row)


### DML 수행

postgres=# \timing on
Timing is on.
postgres=# INSERT INTO repl_test SELECT generate_series(1,10000000) AS id, md5((random()*10000000)::text) AS pad;
INSERT 0 10000000
Time: 32596.670 ms (00:32.597)


### DML 후 lsn

postgres=# select pg_current_wal_lsn();
 pg_current_wal_lsn
--------------------
 7/3CCA51E0
(1 row)


### walsender 확인
$ ps -ef | grep walsender
postgres: walsender repl 22.222.22.22(34856) streaming 7/3CCA51E0

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; commit을 안찍었음에도 replication에 필요한 WAL 을 이미 standby 로 넘기고 Standby에서 WAL 을 재생하고 있는 상태입니다.&lt;br&gt;
commit 을 찍어야 Standby에서 replication data를 반영하는 MySQL과는 차이가 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;standby&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### Primary 에서 insert 중에 실시간으로 WAL 받고 있음 
$ ps -ef | grep wal
postgres: walreceiver   streaming 7/A0000000

$ ps -ef | grep wal
postgres: walreceiver   streaming 7/A4000000

$ ps -ef | grep wal
postgres: walreceiver   streaming 7/A7C00000

$ ps -ef | grep wal
postgres: walreceiver   streaming 7/AAC00000

### Primary 

postgres=# commit;
COMMIT
Time: 0.113 ms

### Standby
postgres=# select count(*) from repl_test;
  count
----------
 10000000
(1 row)

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; Primary 에서 insert 를 완료하는데 30초 가량 걸렸지만 commit 하자마자 Standby에서도 데이터가 모두 반영된것을 확인 할 수 있습니다.&lt;br&gt;
이렇듯 PostgreSQL은 MySQL 처럼 binlog로 SQL 커맨드를 전달하는 게 아니라 트랜잭션 로그인 WAL 을 전달하기 때문에 거의 실시간으로 replication에 필요한 데이터를 전달하고 반영할 수 있습니다.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL to MySQL Migration</title>
      <link>https://kimdubi.github.io/postgresql/pg2mysql/</link>
      <pubDate>Fri, 21 Aug 2020 22:13:24 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/pg2mysql/</guid>
      <description>&lt;p&gt;MySQL을 PostgreSQL 로 이관하는 방법은 PostgreSQL에서 제공하는 기능인 pgloader 를 사용하면 오브젝트부터 데이터까지 쉽게 이관할 수 있지만&lt;br&gt;
반대인 PostgreSQL -&amp;gt; MySQL 은 적당한 툴도 없고, 두 DB의 schema 개념도 달라 사전에 알아둬야 할 점이 많습니다.&lt;br&gt;
이번에 이관했던 서비스를 예시로 PostgreSQL =&amp;gt; MySQL 이관 방법을 정리해보았습니다.&lt;br&gt;
&lt;!-- raw HTML omitted --&gt;&lt;/p&gt;
&lt;h2 id=&#34;postgresql--mysql-schema-&#34;&gt;PostgreSQL 과 MySQL schema 차이&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/p1-5.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;PostgreSQL에서는 테이블의 집합이 schema ( 위 그림에서 public, myschema )&lt;/li&gt;
&lt;li&gt;schema 의 집합이 database ( 위 그림에서 postgres , mydb )&lt;/li&gt;
&lt;li&gt;테이블의 집합 =  schema 가 곧 논리 database 인 MySQL과는 개념이 다름&lt;/li&gt;
&lt;li&gt;단 아래와 같이 DB 내에 사용하는 schema 가 단 하나일 땐 PostgreSQL database -&amp;gt; MySQL database 1:1 매핑이 가능함&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;amon=# \dn
          List of schemas
        Name        |    Owner
--------------------+--------------
 information_schema | testuser
 pg_catalog         | testuser
 pg_toast           | testuser
 pg_toast_temp_1    | testuser
 public             | testuser
(5 rows)

=&amp;gt; public 을 제외한 나머지 schema는 시스템 관련 스키마, pulic은 모든 schema가 접근 가능한 default schema  

test=# select count(*) from information_schema.tables where table_schema=&#39;public&#39;;
 count
-------
   100
(1 row)

=&amp;gt; 해당 서비스에서는 다른 서비스용 schema 를 생성하지 않고 기본 public schema에 오브젝트를 생성하였음
&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;-&#34;&gt;이관대상 확인&lt;/h2&gt;
&lt;h3 id=&#34;database&#34;&gt;database&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# \l+
                                                                  List of databases
   Name    |    Owner     | Encoding | Collation  |   Ctype    |         Access privileges         |  Size   | Tablespace |        Description
-----------+--------------+----------+------------+------------+-----------------------------------+---------+------------+---------------------------
 test1      | testuser      | UTF8     | en_US.UTF8 | en_US.UTF8 |                                   | 13 MB   | pg_default |
 test2      | testuser      | UTF8     | en_US.UTF8 | en_US.UTF8 |                                   | 172 MB  | pg_default |
                                                               : 

 template0 | postres        | UTF8     | en_US.UTF8 | en_US.UTF8 | =c/&amp;quot;testuser&amp;quot;                 | 5408 kB | pg_default |
                                                               : &amp;quot;testuser&amp;quot;=CTc/&amp;quot;testuser&amp;quot;
 template1 | postgres       | UTF8     | en_US.UTF8 | en_US.UTF8 | =c/&amp;quot;testuser&amp;quot;                 | 5408 kB | pg_default | default template database
                                                               : &amp;quot;testuser&amp;quot;=CTc/&amp;quot;testuser&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;이관 대상 PostgreSQL DB 서버 내에 여러 database 들이 존재하는 상황임&lt;/li&gt;
&lt;li&gt;원래 개념대로라면 위의 database 하나하나가 MySQL의 DB instance 로 매핑되지만
아래와 같이 각 database 내에서 schema 를 public schema 하나만 사용하고 있어 PostgreSQL 과 MySQL 1:1 매핑이 가능함&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;schema&#34;&gt;schema&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;test1=# \dn
          List of schemas
        Name        |    Owner
--------------------+--------------
 information_schema | testuser
 pg_catalog         | testuser
 pg_toast           | testuser
 pg_toast_temp_1    | testuser
 public             | testuser
(5 rows)

test1=# select count(*) from information_schema.tables where table_schema=&#39;public&#39;;
 count
-------
    100
(1 row)

test1=# \dn
          List of schemas
        Name        |    Owner
--------------------+--------------
 information_schema | testuser
 pg_catalog         | testuser
 pg_toast           | testuser
 pg_toast_temp_1    | testuser
 public             | testuser
(5 rows)


test2=# select count(*) from information_schema.tables where table_schema=&#39;public&#39;;
 count
-------
    84
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;user&#34;&gt;user&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# select * from pg_catalog.pg_roles;
   rolname    | rolsuper | rolinherit | rolcreaterole | rolcreatedb | rolcatupdate | rolcanlogin | rolconnlimit | rolpassword | rolvaliduntil | rolconfig |  oid
--------------+----------+------------+---------------+-------------+--------------+-------------+--------------+-------------+---------------+-----------+-------
 testuser     | t        | t          | t             | t           | t            | t           |           -1 | ********    |               |           |    10
.
.

&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;pg2mysql&#34;&gt;pg2mysql&lt;/h2&gt;
&lt;p&gt;PostgreSQL dump 파일을 MySQL 쿼리로 변환해주는 툴로 더이상 개발되지 않아 실사용 하기위해 몇가지 부분을 추가하였음&lt;/p&gt;
&lt;h3 id=&#34;pg2mysql----&#34;&gt;pg2mysql  이슈 및 수정사항&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;테이블 정의 중 Constraint 부분을 의도적으로 생략하는 이슈
&lt;ul&gt;
&lt;li&gt;foreign , unique constraint 생성 구문 변환되도록 수정&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Unique key가 일반인덱스로 생성 되는 이슈
&lt;ul&gt;
&lt;li&gt;Unique key로 생성되도록 수정&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;engine 변환 구문 오류
&lt;ul&gt;
&lt;li&gt;default로  InnoDB engine 으로 생성하도록 수정&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;메모리 제한 이슈
&lt;ul&gt;
&lt;li&gt;pg2mysql 프로그램이 사용할 수 있는 최대 메모리 limit 을 512MB로 제한&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;이관했던 서비스가 테이블, 컬럼을 모두 대문자로 사용하여 생성구문을 모두 대문자로 생성하도록 strtoupper 함수를 추가했음
소문자로 사용하는 경우 strtoupper 함수부분을 제거해야함&lt;/li&gt;
&lt;li&gt;cidr, inet 컬럼 미지원
&lt;ul&gt;
&lt;li&gt;PostgreSQL에는 IP를 표현하는 cidr, inet 컬럼 타입이 있으나 MySQL에는 없음, 이부분은 MySQL에서 big int + inet_aton, inet_ation으로 구현할지 varchar 타입으로 전환할지 결정이 필요하여 수정하지 않았음.
참고로, varchar 로 전환시 order by 결과값이 inet 과는 다름&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;FK option
&lt;ul&gt;
&lt;li&gt;PostgreSQL에는 DEFERRABLE INITIALLY DEFERRED ( 제약조건을 트랜잭션 종료할 때 검사하는 설정) 같은  MySQL에서는 지원하지 않는 기능이 있음. 경우에 따라 fk 조건을 삭제하거나 트랜잭션 시작시에 set foreign_key_checks = 0 ;  설정을 주도록 개발팀과 협의가 필요하기 때문에 변환과정에서 생략하지 않고 포함하도록함&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;heading&#34;&gt;사용방법&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;php 설치&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;$ php --version
PHP 5.3.3 (cli) (built: Mar 22 2017 12:27:09)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;첨부파일의 pg2mysql tar 파일을 품&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;$ ls -ltr
total 44
-rw-r--r-- 1 irteam irteam 15145 May 26  2006 gpl.txt
-rw-r--r-- 1 irteam irteam  2370 Jul 21  2011 pg2mysql.php
-rw-r--r-- 1 irteam irteam  1534 Jul 21  2011 pg2mysql_cli.php
-rw-r--r-- 1 irteam irteam  2985 Jul 21  2011 README
-rw-rw-r-- 1 irteam irteam 13204 Aug 10 20:44 pg2mysql.inc.php.org
-rw-r--r-- 1 irteam irteam 13235 Aug 10 20:44 pg2mysql.inc.php
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;이관 대상 PostgreSQL dump 파일 준비&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### schema only
pg_dump -p port -U username -S dbname  --format p -f dbname_schema.sql

### data only, insert 구문으로
pg_dump -p port -U username -a dbname --inserts --format p -f dbname_data.sql

### schema+ data
pg_dump -p port -U username  dbname --inserts --format p -f dbname_full.sql
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;MySQL 쿼리로 변환&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;php pg2mysql_cli.php dbname_full.sql mysql_dbname.sql 
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;--count-&#34;&gt;데이터 검증 (count 확인)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;PostgreSQL&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash
database=database_name
user=postgres
echo &amp;quot;Database : $database&amp;quot;
PGCOMMAND=&amp;quot; psql -U $user -d $database -At -c \&amp;quot;
            SELECT   table_name
            FROM     information_schema.tables
            WHERE    table_type=&#39;BASE TABLE&#39;
            AND      table_schema=&#39;public&#39;
            \&amp;quot;&amp;quot;

TABLENAMES=$(export PGPASSWORD=패스워드; eval &amp;quot;$PGCOMMAND&amp;quot;)

for TABLENAME in $TABLENAMES; do
    PGCOMMAND=&amp;quot; psql -U $user -d $database -At -c \&amp;quot;  
                SELECT   &#39;$TABLENAME&#39;,  
                         count(*)   
                FROM     $TABLENAME  
                \&amp;quot;&amp;quot;
    eval &amp;quot;$PGCOMMAND&amp;quot;
done
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;MySQL&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash
database=database_name
for name in `mys $database -B -e &amp;quot;show tables&amp;quot;`;
do
   mys -B --vertical $database -e &amp;quot;select count(*) as $name from $name&amp;quot; | grep -v &#39;^\*&#39; | replace &amp;quot;: &amp;quot; &amp;quot;|&amp;quot;
done
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;index count&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;* index count

# MySQL
select table_name,count(distinct index_name) from information_schema.STATISTICS where index_schema=&#39;amon&#39; group by 1 order by 1;

# PostgreSQL
select tablename,count(*) from pg_indexes where schemaname=&#39;public&#39; group by 1 order by 1;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; MySQL로 변환되면서 Auto-increment 컬럼이 PK 가 되기 때문에 MySQL 이 인덱스가 더 많을 수 있음&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>pg_replication_slot 사용 시 wal logfile이 계속 쌓일때</title>
      <link>https://kimdubi.github.io/postgresql/pg_slot_issue/</link>
      <pubDate>Thu, 20 Aug 2020 15:19:06 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/pg_slot_issue/</guid>
      <description>&lt;p&gt;PostgreSQL 의 복제구성으로 가장 널리 쓰이는 방법은 streaming replication + physical replication slot 입니다.&lt;br&gt;
특히 physical replication slot 을 사용하는 경우에는 DB에서 slave 가 필요로 하는 WAL logfile 정보를 관리하기 때문에&lt;br&gt;
운영자가 wal_keep_segments , max_wal_size 같은 WAL 보관 설정에 대해 고민할 필요가 없습니다.&lt;br&gt;
&lt;!-- raw HTML omitted --&gt;&lt;br&gt;
그러나 이 편리한 replication slot 기능에도 예외상황이 있는데요&lt;br&gt;
slave DB가 계속 replication 을 받지 못하는 상황이거나, 사용하지 않는 replication slot이 방치 된 경우&lt;br&gt;
DB는 WAL logfile들이 나중엔 반영될 거라고 보기 때문에 계속~ 무한정 ~ file들을 저장하게 됩니다. 때문에 서버 디스크 이슈가 발생할 수 있습니다.&lt;/p&gt;
&lt;h3 id=&#34;wal---&#34;&gt;WAL 보관 설정 확인&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# select name,setting from pg_settings where name in (&#39;max_wal_size&#39;,&#39;min_wal_size&#39;,&#39;wal_keep_segments&#39;,&#39;wal_segment_size&#39;);
       name        | setting
-------------------+----------
 max_wal_size      | 128
 min_wal_size      | 128
 wal_keep_segments | 0
 wal_segment_size  | 16777216
(4 rows)
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;replication-slot---&#34;&gt;replication slot 비활성 상태일 때&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE TABLE t(id INT, pad CHAR(200)); 
CREATE INDEX t_id ON t (id); 
INSERT INTO t SELECT generate_series(1,1000000) AS id, md5((random()*1000000)::text) AS pad;

UPDATE t SET pad = md5((random()*1000000)::text);  &amp;lt;== 반복 수행


$ du -sh pg_wal
129M    pg_wal
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; 대량의 WAL log를 떨구는 커맨드를 수행해도 max_wal_size 설정을 초과해서 파일이 쌓이지 않습니다.&lt;/p&gt;
&lt;h3 id=&#34;replication-slot-----&#34;&gt;replication slot이 활성 상태지만, 복제는 안되는 상황일때&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# select * from pg_replication_slots;
-[ RECORD 1 ]-------+-------------
slot_name           | repl_slot_01
plugin              |
slot_type           | physical
datoid              |
database            |
temporary           | f
active              | f
active_pid          |
xmin                |
catalog_xmin        |
restart_lsn         | 4/F90000D0
confirmed_flush_lsn |

=&amp;gt; active f , active_pid NULL ,

postgres=# UPDATE t SET pad = md5((random()*1000000)::text);
UPDATE 1000000


$ du -sh pg_wal
145M    pg_wal
$ du -sh pg_wal
209M    pg_wal
.
.
$ du -sh pg_wal
2.0G    pg_wal

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; replication_slot 활성화 상태라서 slave 에 반영안된 wal logfile들을 쭉 보관하게됩니다&lt;br&gt;
이때는 max_wal_size, wal_keep_segments 설정과는 상관없으며 이처럼 replication_slot 이 활성화 된 상태에서&lt;br&gt;
replication 만 안되는 상황에서는 디스크 용량 이슈가 발생할 수 있습니다.&lt;/p&gt;
&lt;h3 id=&#34;replication-slot-----1&#34;&gt;replication slot이 활성 상태, 복제도 정상인 상황일때&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# SELECT redo_lsn, slot_name,restart_lsn,
round((redo_lsn-restart_lsn) / 1024 / 1024 / 1024, 2) AS GB_behind
FROM pg_control_checkpoint(), pg_replication_slots;
-[ RECORD 1 ]-------------
redo_lsn    | 5/732000B0
slot_name   | repl_slot_01
restart_lsn | 4/F90000D0
gb_behind   | 1.91

=&amp;gt; slave 에 반영안되어서 보관하고 있는 wal logfile 이 2GB

slave) pg_ctl -D /home1/irteam/psql/engn/PGSQL start


postgres=# SELECT redo_lsn, slot_name,restart_lsn,
round((redo_lsn-restart_lsn) / 1024 / 1024 / 1024, 2) AS GB_behind
FROM pg_control_checkpoint(), pg_replication_slots;
-[ RECORD 1 ]-------------
redo_lsn    | 5/9A1FEB88
slot_name   | repl_slot_01
restart_lsn | 5/A0C6A000
gb_behind   | -0.10

=&amp;gt; 기동 후 wal logfile을 반영하면서 보관 중이던 wal logfile도 정리됨

$ du -sh pg_wal
129M    pg_wal

&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;heading&#34;&gt;정리&lt;/h3&gt;
&lt;p&gt;pg_replication_slots 기능은 DB가 필요한 wal logfile을 관리해주기 때문에 편리한 기능이지만&lt;br&gt;
replication_slot이 구성된 slave db의 다운 상태가 길어질 수록 예기치 못한 디스크 용량 이슈가 발생할 수 있어 주의가 필요합니다.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL schema 의미 및 권한관리</title>
      <link>https://kimdubi.github.io/postgresql/pg_schema/</link>
      <pubDate>Sun, 09 Aug 2020 15:01:43 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/pg_schema/</guid>
      <description>&lt;p&gt;MySQL이나 ORACLE 을 다루다가 PostgreSQL을 처음 다룰 때 가장 헷갈리는 것 중에 하나는 바로 schema의 개념입니다.&lt;br&gt;
ORACLE에서는 오브젝트를 가진 USER, MySQL은 논리DB를 schema 라고 하는 반면에&lt;br&gt;
PostgreSQL 에서는 database , schema , user 의 개념이 모두 있기 때문에 처음엔 헷갈릴 수 있는데요&lt;br&gt;
이번 글에서는 PostgreSQL의 schema 와 권한 관리에 대해 알아보겠습니다.&lt;/p&gt;
&lt;h2 id=&#34;postgresql-schema&#34;&gt;PostgreSQL Schema&lt;/h2&gt;
&lt;p&gt;MySQL에서는 논리 Database를 schema 와 같은 의미로 사용합니다.
반면 PostgreSQL에서는 database와 schema 두가지 개념 모두 사용되며 database는 schema의 상위 개념이라 할 수 있습니다.&lt;br&gt;
table의 집합을 schema 라고 표현하며 이 schema는 하나의 database를 논리적으로 나누는 개념입니다. 즉, MySQL에서의 논리 database는 PostgreSQL에서의 schema라고 할 수 있습니다.&lt;/p&gt;
&lt;p&gt;이런 차이점 때문에 PostgreSQL에서는 하나의 DB instance에 있다해도 서로 다른 database에 있는 테이블 간에는 서로 JOIN 연산을 할 수 없습니다.&lt;br&gt;
대신 서로 다른 schema의 테이블 간에는 JOIN 연산이 가능합니다.&lt;/p&gt;
&lt;p&gt;PostgreSQL에서 database 와 schema를 생성해보면서 더 자세히 확인해보겠습니다.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;### testdb의 owner가 될 testuser 생성
postgres=# create user testuser with password &#39;qhdks123&#39;;
CREATE ROLE

### testdb 생성

postgres=# create database testdb with owner testuser;
CREATE DATABASE
postgres=# \l+
                                                                   List of databases
   Name    |  Owner   | Encoding |  Collate   |   Ctype    |   Access privileges   |  Size   | Tablespace |                Description
-----------+----------+----------+------------+------------+-----------------------+---------+------------+--------------------------------------------
 postgres  | postgres | UTF8     | en_US.utf8 | en_US.utf8 |                       | 7945 kB | pg_default | default administrative connection database
 template0 | postgres | UTF8     | en_US.utf8 | en_US.utf8 | =c/postgres          +| 7801 kB | pg_default | unmodifiable empty database
           |          |          |            |            | postgres=CTc/postgres |         |            |
 template1 | postgres | UTF8     | en_US.utf8 | en_US.utf8 | =c/postgres          +| 7801 kB | pg_default | default template for new databases
           |          |          |            |            | postgres=CTc/postgres |         |            |
 testdb    | testuser | UTF8     | en_US.utf8 | en_US.utf8 |                       | 7801 kB | pg_default |
(4 rows)

### testuser 계정으로 testdb 접속

postgres-# \c testdb testuser
You are now connected to database &amp;quot;testdb&amp;quot; as user &amp;quot;testuser&amp;quot;.

### testdb 내 testdb , testdb_log schema 생성

testdb=&amp;gt; create schema testdb authorization testuser;
CREATE SCHEMA

testdb=&amp;gt; create schema testdb_log authorization testuser;
CREATE SCHEMA

testdb=&amp;gt; \dn+
                            List of schemas
    Name    |  Owner   |  Access privileges   |      Description
------------+----------+----------------------+------------------------
 public     | postgres | postgres=UC/postgres+| standard public schema
            |          | =UC/postgres         |
 testdb     | testuser |                      |
 testdb_log | testuser |                      |
(3 rows)

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; testdb 라는 database 를 생성하고 그 안에 testdb , testdb_log 라는 schema를 생성했습니다.&lt;br&gt;
그리고 schema 리스트를 확인해보니 public 이라는 schema 가 보이는데 이는 default로 생성되는 schema로 별다른 schema를 지정하지 않으면&lt;br&gt;
이 public schema에 오브젝트를 사용하게 됩니다. public schema에 오브젝트를 생성하게 되면 보안 관리가 안되기 때문에 public schema 권한을 revoke 하는데요&lt;br&gt;
이 부분은 이따 권한 관리 부분에서 살펴보겠습니다.&lt;/p&gt;
&lt;p&gt;schema를 생성했으니 해당 schema에 오브젝트를 생성해 보겠습니다.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;testdb=&amp;gt; create table tb_test (a int);
CREATE TABLE
testdb=&amp;gt; \dt
          List of relations
 Schema |  Name   | Type  |  Owner
--------+---------+-------+----------
 public | tb_test | table | testuser
(1 row)

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; 위와 같이 단순히 create table을 하게 되면 public schema에 생성됩니다.&lt;br&gt;
postgresql에는 search_path 라는 테이블을 어떤 schema에서 우선적으로 찾으면 되는지 설정하는 기능이 있으므로 이 부분을 설정해보겠습니다.&lt;br&gt;
MySQL에서 &lt;strong&gt;use database ;&lt;/strong&gt; 커맨드와 동일한 기능입니다.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;### search_path 를 testdb schema 로 설정

testdb=&amp;gt; set search_path to &amp;quot;$user&amp;quot;,testdb;
SET

testdb=&amp;gt; create table tb_test (a int);
CREATE TABLE

testdb=&amp;gt; \dt
          List of relations
 Schema |  Name   | Type  |  Owner
--------+---------+-------+----------
 testdb | tb_test | table | testuser
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;지금까지 수행한 PostgreSQL의 커맨드들은 MySQL과 비교하면 아래와 같습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;create database&lt;/li&gt;
&lt;li&gt;create schema&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### PostgreSQL
testdb=&amp;gt; create schema testdb authorization testuser;
CREATE SCHEMA

testdb=&amp;gt; \dn+
                            List of schemas
    Name    |  Owner   |  Access privileges   |      Description
------------+----------+----------------------+------------------------
 public     | postgres | postgres=UC/postgres+| standard public schema
            |          | =UC/postgres         |
 testdb     | testuser |                      |

### MySQL
mysql&amp;gt; create database testdb;
Query OK, 1 row affected (0.01 sec)

mysql&amp;gt; show databases;
+--------------------+
| Database           |
+--------------------+
| testdb             |
+--------------------+
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;set search_path&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### PostgreSQL
testdb=&amp;gt; show search_path;
 search_path
-------------
 public
(1 row)

testdb=&amp;gt; set search_path to testdb;
SET

testdb=&amp;gt; show search_path;
 search_path
-------------
 testdb
(1 row)

testdb=&amp;gt; \dt
          List of relations
 Schema |   Name   | Type  |  Owner
--------+----------+-------+----------
 testdb | tb_test  | table | testuser
 testdb | tb_test2 | table | testuser
 testdb | tb_test3 | table | testuser
(3 rows)

### MySQL
mysql&amp;gt; use testdb;
Database changed

mysql&amp;gt; show tables;
+------------------+
| Tables_in_testdb |
+------------------+
| gtid_test        |
| tb_test          |
+------------------+
2 rows in set (0.00 sec)

&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;postgresql--&#34;&gt;PostgreSQL 권한 관리&lt;/h2&gt;
&lt;p&gt;PostgreSQL은 MySQL 과 database, schema 개념이 약간 다르다보니 권한관리 하는 범위도 다릅니다.&lt;br&gt;
서비스와 상관 없는 다른 user들의 접근을 막고, DDL 가능 계정, 서비스용도의 DML 계정으로 권한을 관리해보겠습니다.&lt;/p&gt;
&lt;h3 id=&#34;-user--&#34;&gt;다른 USER 접근 막기&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# create user newuser with password &#39;qhdks123&#39;;
CREATE ROLE

postgres=# \c testdb newuser
You are now connected to database &amp;quot;testdb&amp;quot; as user &amp;quot;newuser&amp;quot;.

testdb=&amp;gt; create table tb_test10 (a int);
CREATE TABLE
testdb=&amp;gt; \dt
           List of relations
 Schema |   Name    | Type  |  Owner
--------+-----------+-------+----------
 public | tb_test   | table | testuser
 public | tb_test10 | table | newuser
(2 rows)

testdb=&amp;gt; create table testdb.tb_test10 (a int);
ERROR:  permission denied for schema testdb

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; testdb 서비스와 상관없는 newuser가 testdb 에 접근 및 public schema에 오브젝트도 생성할 수 있는 상황&lt;br&gt;
testdb schema에는 권한이 없음&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;testdb=# \c postgres postgres
You are now connected to database &amp;quot;postgres&amp;quot; as user &amp;quot;postgres&amp;quot;.

postgres=# revoke all on database testdb from PUBLIC;
REVOKE

postgres=# \c testdb newuser
FATAL:  permission denied for database &amp;quot;testdb&amp;quot;
DETAIL:  User does not have CONNECT privilege.
Previous connection kept

postgres=# \c testdb testuser
You are now connected to database &amp;quot;testdb&amp;quot; as user &amp;quot;testuser&amp;quot;.
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; 소유자가 아닌 계정의 접근을 막음&lt;/p&gt;
&lt;h3 id=&#34;ddl--dml--&#34;&gt;DDL / DML 계정 분리&lt;/h3&gt;
&lt;p&gt;DDL용 계정은 SERVICE_OWNER&lt;br&gt;
DML용 계정 : SERVICE_USER&lt;br&gt;
서비스 DB : DBNAME&lt;br&gt;
서비스 schema : SERVICE_OWNER&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;신규 유저 생성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;psql -d postgres -U postgres -c &amp;quot;create user ${SERVICE_OWNER} with password &#39;qhdks123&#39;&amp;quot;
psql -d postgres -U postgres -c &amp;quot;create user ${SERVICE_USER} with password &#39;qhdks123&#39;&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;신규 DB 생성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;psql -d postgres -U postgres -c &amp;quot;create database ${DBNAME} with OWNER=${SERVICE_OWNER}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;db 접근 권한 및 public schema 접근 권한 revoke&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;psql -d ${DBNAME} -U ${SERVICE_OWNER} -c &amp;quot;revoke all on database ${DBNAME} from public&amp;quot;
psql -d ${DBNAME} -U postgres    -c &amp;quot;revoke all on schema public from public&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;신규db에 schema 생성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;psql -d ${DBNAME} -U ${SERVICE_OWNER} -c &amp;quot;create schema ${SERVICE_OWNER} authorization ${SERVICE_OWNER}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;서비스용 유저에 최소한의 권한만 부여&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;psql -d ${DBNAME} -U ${SERVICE_OWNER} -c &amp;quot;grant connect,TEMPORARY on database ${DBNAME} to ${SERVICE_USER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER} -c &amp;quot;grant usage on schema ${SERVICE_OWNER} to ${SERVICE_USER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER}  -c &amp;quot;alter role ${SERVICE_USER} set search_path to ${SERVICE_OWNER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER}  -c &amp;quot;grant select, insert, update, delete on all tables in schema ${SERVICE_OWNER} to ${SERVICE_USER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER}  -c &amp;quot;alter default privileges in schema ${SERVICE_OWNER} grant select, insert, update, delete on tables to ${SERVICE_USER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER}  -c &amp;quot;grant usage on all sequences in schema ${SERVICE_OWNER} to ${SERVICE_USER}&amp;quot;
psql -d ${DBNAME} -U ${SERVICE_OWNER}  -c &amp;quot;alter default privileges in schema ${SERVICE_OWNER} grant usage on sequences to ${SERVICE_USER}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;참고
&lt;a href=&#34;https://m.blog.naver.com/hanccii/221701395102&#34;&gt;https://m.blog.naver.com/hanccii/221701395102&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL background process 살펴보기</title>
      <link>https://kimdubi.github.io/postgresql/postgresql_bg_process/</link>
      <pubDate>Sat, 08 Aug 2020 23:52:20 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/postgresql_bg_process/</guid>
      <description>&lt;h2 id=&#34;heading&#34;&gt;시작하며&lt;/h2&gt;
&lt;p&gt;PostgreSQL의 background process는 기본 프로세스인 Postmaster에서 fork() 된 프로세스들입니다.&lt;br&gt;
각 fork() 된 background process들은 DBMS 운영을 위해 각자 맡은 역할을 수행하게 되는데요.&lt;br&gt;
지난 글에서 PostgreSQL의 architecture에 대해 살펴보며 간략하게 말씀드렸지만&lt;br&gt;
이번 글에서는 PostgreSQL의 background process들에 대해 좀 더 자세히 살펴보겠습니다.&lt;/p&gt;
&lt;h2 id=&#34;postgresql-background-process&#34;&gt;PostgreSQL background process&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_bgprocess.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;h3 id=&#34;postmaster&#34;&gt;Postmaster&lt;/h3&gt;
&lt;p&gt;Postmaster 는 모든 background process들을 제어하고 client =&amp;gt; DBMS로의 connection을 생성하고 닫아주는 최초의 프로세스입니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;DB 기동 시 Postmaster는 다른 background process를 fork() 하는 역할과&lt;/li&gt;
&lt;li&gt;client가 PostgreSQL DBMS에 connection을 요청하면 Postmaster는 설정된 인증방법(pg_hba.conf)에 따라 사용자를 인증하고&lt;br&gt;
인증을 통과하면 사용자에게 서비스를 제공하기 위한 새로운 session을 fork() 하는 역할을 합니다.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;bgwriter&#34;&gt;BgWriter&lt;/h3&gt;
&lt;p&gt;BgWriter 프로세스는 Shared Memory 내 dirty page를 디스크에 내려쓰는 프로세스입니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;shared buffer의 dirty page를 디스크로 주기적으로 flush 수행&lt;/li&gt;
&lt;li&gt;주기적인 checkpoint 발생 시 모든 dirty page를 디스크에 기록&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;이 중 첫번째 작업은 dirty page를 미리 조금씩 flush 해놔서 checkpoint 동작 때 I/O가 몰리지 않도록 하자&lt;br&gt;
즉, 시스템 I/O load 의 부담을 줄이기 위한 동작이라고 볼 수 있습니다.&lt;/p&gt;
&lt;h4 id=&#34;-parameter&#34;&gt;관련 parameter&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;#bgwriter_delay = 200ms                 # 10-10000ms between rounds
#bgwriter_lru_maxpages = 100            # max buffers written/round, 0 disables
#bgwriter_lru_multiplier = 2.0          # 0-10.0 multiplier on buffers scanned/round
#bgwriter_flush_after = 512kB           # measured in pages, 0 disables
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;bgwriter_delay : 두 번의 연속적인 flush 동작 간 delay 설정&lt;/li&gt;
&lt;li&gt;bgwriter_lru_maxpages : bgwriter가 한번에 내려쓰는 데이터 단위로 dirty page가 100 보다 더 많으면 서버 프로세스에서 처리됨&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;walwriter&#34;&gt;WalWriter&lt;/h3&gt;
&lt;p&gt;Write Ahead Logging의 개념은 데이터 파일의 수정은 변경 사항이 로그에 먼저 기록 된 후에만 이루어져야한다는 것입니다.&lt;br&gt;
이 메커니즘을 사용하면 디스크에 데이터를 자주 내려 쓰지 않고 disk I/O를 줄일 수 있습니다.&lt;br&gt;
또한 WAL 로그를 사용하여 dbms 를 최근 시점까지도 복구할 수 있습니다.&lt;/p&gt;
&lt;h4 id=&#34;-&#34;&gt;관련 파라미터&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;#------------------------------------------------------------------------------
# WRITE-AHEAD LOG
#------------------------------------------------------------------------------

# - Settings -

wal_level = logical                     # minimal, replica, or logical
                                        # (change requires restart)
#fsync = on                             # flush data to disk for crash safety
                                        # (turning this off can cause
                                        # unrecoverable data corruption)
#synchronous_commit = on                # synchronization level;
                                        # off, local, remote_write, remote_apply, or on
#wal_sync_method = fsync                # the default is the first option
                                        # supported by the operating system:
                                        #   open_datasync
                                        #   fdatasync (default on Linux)
                                        #   fsync
                                        #   fsync_writethrough
                                        #   open_sync
#full_page_writes = on                  # recover from partial page writes
#wal_compression = off                  # enable compression of full-page writes
#wal_log_hints = off                    # also do full page writes of non-critical updates
                                        # (change requires restart)
#wal_init_zero = on                     # zero-fill new WAL files
#wal_recycle = on                       # recycle WAL files
#wal_buffers = -1                       # min 32kB, -1 sets based on shared_buffers
                                        # (change requires restart)
#wal_writer_delay = 200ms               # 1-10000 milliseconds
#wal_writer_flush_after = 1MB           # measured in pages, 0 disables

#commit_delay = 0                       # range 0-100000, in microseconds
#commit_siblings = 5                    # range 1-1000
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;wal_level : WAL logfile에 기록되는 정보의 수준, 기본값인 replica 부터 streaming replication 과 hot standby를 사용할 수 있음&lt;/li&gt;
&lt;li&gt;fsync : 변경사항을 WAL logfile 로 내려쓰겠다는 설정, off 설정 시 성능 상 이점이 있으나 crash 발생하면 데이터 손실남&lt;/li&gt;
&lt;li&gt;synchronous_commit : ON 설정 시 변경사항이 WAL logfile 에 기록되어야 트랜잭션 완료&lt;/li&gt;
&lt;li&gt;wal_sync_method : 변경사항을 WAL logfile로 내려쓰는 방법, fsync, fdatasync 등이 있음&lt;/li&gt;
&lt;li&gt;full_page_writes : 전체 data page를 WAL log에 쓰도록 설정&lt;/li&gt;
&lt;li&gt;wal_buffers : WAL log를 저장하는 데 사용되는 메모리 공간, wal_writer_delay, commit_delay 설정에 영향받을 수 있음&lt;/li&gt;
&lt;li&gt;wal_writer_delay : walwriter 프로세스의 동작 간격 설정으로, 시간이 너무 길면 WAL buffer가 부족할 수 있으며 시간이 너무 짧으면 WAL을 계속 쓰게되어 디스크 I/O 부담이 증가할 수 있음&lt;/li&gt;
&lt;li&gt;commit_delay : commit 이후 WAL flush 되기 전 WAL buffer에서 대기하는 시간, group commit 과 같은 효과&lt;/li&gt;
&lt;li&gt;commit_siblings : commit delay 설정을 수행하기 전, 필요한 최소 동시 트랜잭션 수, 트랜잭션 수가 commit_siblings 보다 적은 경우 바로 WAL flush 됨.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;pgarch-&#34;&gt;PgArch 프로세스&lt;/h3&gt;
&lt;p&gt;WAL logfile은 재활용 되기 때문에 과거의 WAL logifle은 새로운 log로 덮어 씌여집니다.&lt;br&gt;
중간에 WAL logfile이 유실되는 경우 원하는 시점으로 시점복구가 어려울 수 있기 때문에 PostgreSQL에서는
WAL logfile을 백업해놓을 수 있는데 이때 역할을 수행하는 프로세스가 PgArch 프로세스입니다.&lt;/p&gt;
&lt;h4 id=&#34;-1&#34;&gt;관련 파라미터&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;# - Archiving -

#archive_mode = off             # enables archiving; off, on, or always
                                # (change requires restart)
#archive_command = &#39;&#39;           # command to use to archive a logfile segment
                                # placeholders: %p = path of file to archive
                                #               %f = file name only
                                # e.g. &#39;test ! -f /mnt/server/archivedir/%f &amp;amp;&amp;amp; cp %p /mnt/server/archivedir/%f&#39;
#archive_timeout = 0            # force a logfile segment switch after this
                                # number of seconds; 0 disables
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;archive_mode : WAL logfile을 백업하는 archive_mode 설정 여부&lt;/li&gt;
&lt;li&gt;archive_command : WAL 로그를 백업하기 위해 실행 될 명령어. %p : 아카이브해야하는 WAL logfile의 경로, %f : 파일 이름&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;autovacuum-&#34;&gt;AutoVacuum 프로세스&lt;/h3&gt;
&lt;p&gt;PostgreSQL 은 UPDATE, DELETE 작업을 수행 한 후 이전 버전의 데이터를 즉시 삭제하지 않고 레코드에 저장해 놓습니다.&lt;br&gt;
대신, 데이터는 PostgreSQL의 MVCC 메커니즘에 의해 삭제 된 것으로 표시하고 이전 버전의 데이터에 다른 트랜잭션이 액세스하는 경우 임시로 유지합니다.&lt;br&gt;
트랜잭션이 commit 된 후에는 이전 버전의 데이터가 더 이상 필요하지 않으므로 (데드 튜플) PostgreSQL은 공간 및 xid 확보를 위해 데이터를 정리해야합니다.&lt;br&gt;
이 작업은 AutoVacuum 프로세스에서 수행됩니다.&lt;/p&gt;
&lt;h4 id=&#34;-2&#34;&gt;관련 파라미터&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;#------------------------------------------------------------------------------
# AUTOVACUUM
#------------------------------------------------------------------------------

#autovacuum = on                        # Enable autovacuum subprocess?  &#39;on&#39;
                                        # requires track_counts to also be on.
#log_autovacuum_min_duration = -1       # -1 disables, 0 logs all actions and
                                        # their durations, &amp;gt; 0 logs only
                                        # actions running at least this number
                                        # of milliseconds.
#autovacuum_max_workers = 3             # max number of autovacuum subprocesses
                                        # (change requires restart)
#autovacuum_naptime = 1min              # time between autovacuum runs
#autovacuum_vacuum_threshold = 50       # min number of row updates before
                                        # vacuum
#autovacuum_analyze_threshold = 50      # min number of row updates before
                                        # analyze
#autovacuum_vacuum_scale_factor = 0.2   # fraction of table size before vacuum
#autovacuum_analyze_scale_factor = 0.1  # fraction of table size before analyze
#autovacuum_freeze_max_age = 200000000  # maximum XID age before forced vacuum
                                        # (change requires restart)
#autovacuum_multixact_freeze_max_age = 400000000        # maximum multixact age
                                        # before forced vacuum
                                        # (change requires restart)
#autovacuum_vacuum_cost_delay = 2ms     # default vacuum cost delay for
                                        # autovacuum, in milliseconds;
                                        # -1 means use vacuum_cost_delay
#autovacuum_vacuum_cost_limit = -1      # default vacuum cost limit for
                                        # autovacuum, -1 means use
                                        # vacuum_cost_limit
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;autovacuum : autovacuum 프로세스를 사용할지 여부&lt;/li&gt;
&lt;li&gt;autovacuum_max_workers : autovacuum 을 병렬로 수행할 최대 서브 프로세스 수 설정&lt;/li&gt;
&lt;li&gt;autovacuum_naptime : 두 autovacuum 프로세스 사이의 간격 시간을 설정&lt;/li&gt;
&lt;li&gt;autovacuum_vacuum_threshold / autovacuum_analyze_threshold : 테이블에서 업데이트 된 튜플 수의 임계 값 설정 , 튜플 업데이트 수가 이 값을 초과하면 vacuum / analyze 수행 필요&lt;/li&gt;
&lt;li&gt;autovacuum_vacuum_scale_factor / autovacuum_analyze_scale_factor : 설정한 배율만큼 테이블 내 데이터 변경 시, autovacuum이 수행될 테이블로 추가됨&lt;/li&gt;
&lt;li&gt;autovacuum_freeze_max_age : 테이블의 최대 유효 xid, 이 값 이상으로 xid가 커지지 않도록 vacuum 수행함&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;-3&#34;&gt;통계 수집기&lt;/h3&gt;
&lt;p&gt;PostgreSQL optimizer의 쿼리 최적화를 위해 테이블 메타정보, 삭제 / 업데이트 수, 데이터 블록 수, 인덱스 변경 등과 같은 데이터베이스 작업 중 통계 정보를 수집하는 프로세스입니다.&lt;/p&gt;
&lt;h4 id=&#34;-4&#34;&gt;관련 파라미터&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;#------------------------------------------------------------------------------
# STATISTICS
#------------------------------------------------------------------------------

# - Query and Index Statistics Collector -

#track_activities = on
#track_counts = on
#track_io_timing = off
#track_functions = none                 # none, pl, all
#track_activity_query_size = 1024       # (change requires restart)
#stats_temp_directory = &#39;pg_stat_tmp&#39;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;track_activities : 세션에서 현재 실행중인 명령에 대한 통계 정보 수집 기능을 사용할지 여부&lt;/li&gt;
&lt;li&gt;track_counts : 데이터베이스 내 모든 작업에 대한 통계 정보 수집 기능을 사용할지 여부&lt;/li&gt;
&lt;li&gt;track_io_timing : 쿼리 수행 시 I/O 소요시간을 수집함&lt;/li&gt;
&lt;li&gt;track_functions : 함수 호출 수 및 수행 시간 수집&lt;/li&gt;
&lt;li&gt;track_activity_query_size : 각 세션에서 현재 실행 중인 커맨드의 텍스트를 저장하는데 사용할 메모리&lt;/li&gt;
&lt;li&gt;stats_temp_directory : OS 상 통계 정보의 임시 저장 경로&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;checkpointer&#34;&gt;checkpointer&lt;/h3&gt;
&lt;p&gt;checkpoint는 DBMS가 설정하는 트랜잭션 포인트입니다. checkpoint가 실행되면 checkpoint 이전의 모든 변경사항이 디스크로 flush 됩니다.&lt;br&gt;
DBMS가 crash 된 후 복구할 때도 checkpoint 이후 시점의 WAL log를 반영하여 복구하게됩니다.&lt;/p&gt;
&lt;h4 id=&#34;-5&#34;&gt;관련 파라미터&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;# - Checkpoints -

#checkpoint_timeout = 5min              # range 30s-1d
max_wal_size = 1GB
min_wal_size = 80MB
#checkpoint_completion_target = 0.5     # checkpoint target duration, 0.0 - 1.0
#checkpoint_flush_after = 256kB         # measured in pages, 0 disables
#checkpoint_warning = 30s               # 0 disables
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;checkpoint_timeout : 설정한 시간 마다 checkpoint 수행&lt;/li&gt;
&lt;li&gt;max_wal_size : WAL logfile 의 사이즈가 1GB 이상 되면 checkpoint 수행&lt;/li&gt;
&lt;li&gt;min_wal_size : WAL logfile 사이즈가 해당 값 미만이면 checkpoint 발생시 WAL logfile을 지우지 않고 재활용함&lt;/li&gt;
&lt;li&gt;checkpoint_completion_target : 다음 checkpoint 가 발생하기 전 checkpoint_timeout * checkpoint_completion_target 시간 안에 checkpoint 완료되도록 목표 지정하는 설정&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL을 소개합니다</title>
      <link>https://kimdubi.github.io/postgresql/psql_intro/</link>
      <pubDate>Sat, 08 Aug 2020 23:27:44 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_intro/</guid>
      <description>&lt;h2 id=&#34;heading&#34;&gt;시작하며&lt;/h2&gt;
&lt;p&gt;세상엔 저마다의 특징과 장점을 뽐내는 다양한 DBMS들이 있습니다.&lt;br&gt;
이들 각각의 특장점이 무엇인지 파악하고 자신의 서비스에서 어떤 DBMS가 최고의 퍼포먼스를 낼 수 있는지 파악하는 것이 중요하기 때문에&lt;br&gt;
다양한 DBMS 에 대한 이해가 점점 중요해지고 있습니다.&lt;/p&gt;
&lt;p&gt;ORACLE은 강력한 엔터프라이즈 개발 기능과 안정성을, MySQL은 대표적인 오픈소스 DB로 뛰어난 OLTP 성능을 보여주고, CUBRID는 뛰어난 부하 분산 기능을 자랑하는 DBMS입니다.&lt;br&gt;
그리고 이 글에서 소개해드릴 PostgreSQL은 GIS 지리 정보 처리와 엔터프라이즈급 DBMS의 기능을 제공하는 오픈소스 DBMS입니다.&lt;/p&gt;
&lt;p&gt;PostgreSQL은 이미 북미와 일본쪽에서는 굉장히 인기가 많은 DB로 자리잡은데 비해&lt;br&gt;
우리나라에서는 GIS 용도로 굳어버린 이미지 때문인지 활용도가 높은 DB는 아닌데요.&lt;br&gt;
GIS 외에도 기능과 성능면에서 굉장히 뛰어난 오픈소스 DB이기 때문에 알아둘 가치가 충분한 DB입니다.&lt;/p&gt;
&lt;p&gt;이번 글에서는 PostgreSQL의 Architecture에 대해 다루면서 이게 어떤 DBMS인지 알아보겠습니다.&lt;/p&gt;
&lt;h2 id=&#34;postgresql&#34;&gt;PostgreSQL&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_0808_1.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;PostgreSQL&lt;/strong&gt;은 객체관계형 데이터베이스 (ORDBMS) 로 엔터프라이즈급 DBMS의 기능과 뛰어난 GIS 기능으로 유명한 오픈소스DB입니다.&lt;br&gt;
객체지향 모델을 지원하기 때문에 개발자 스스로 어플리케이션에 필요한 data type과 method를 정의할 수 있습니다.&lt;br&gt;
PostgreSQL이 GIS 분야에서 뛰어난 이유도 ORDBMS의 특성을 살려 Native 수준으로 Spatial SQL 요소들을 구현한 &lt;strong&gt;PostGIS&lt;/strong&gt; 라는 Add-on 이 있기 때문입니다.&lt;/p&gt;
&lt;p&gt;1986년부터 30년이상 활발하게 개발되고 있는 PostgreSQL은 현재 13.1 beta 버전이 출시되었으며&lt;br&gt;
MVCC, 트랜잭션 ACID 등 기본적인 RDBMS 기능 뿐만 아니라 &lt;strong&gt;extension&lt;/strong&gt;이라 불리는 확장 모듈 API를 통해 다양한 기능을 제공하고 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;PostgreSQL 소개 : &lt;a href=&#34;https://www.postgresql.org/about/&#34;&gt;https://www.postgresql.org/about/&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;PostgreSQL extension list : &lt;a href=&#34;https://postgresql.kr/docs/current/contrib.html&#34;&gt;https://postgresql.kr/docs/current/contrib.html&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;postgresql-architecture&#34;&gt;PostgreSQL Architecture&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_0808_2.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;PostgreSQL의 아키텍처입니다&lt;/p&gt;
&lt;p&gt;ORACLE 아키텍처에 대해 공부해보신 분이라면 바로 느끼시겠지만 전체적인 아키텍처가 ORACLE과 굉장히 유사합니다.&lt;br&gt;
&lt;strong&gt;Shared Memory&lt;/strong&gt;라는 공유메모리가 있고 PostgreSQL 운영에 필요한 &lt;strong&gt;Background process&lt;/strong&gt;들과 &lt;strong&gt;Data file&lt;/strong&gt;들로 구성됩니다.&lt;/p&gt;
&lt;h3 id=&#34;shared-memory&#34;&gt;&lt;strong&gt;Shared Memory&lt;/strong&gt;&lt;/h3&gt;
&lt;p&gt;Shared Memory에서 가장 중요한 요소는 &lt;strong&gt;Shared Buffer&lt;/strong&gt;와 &lt;strong&gt;WAL buffer&lt;/strong&gt;입니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Shared Buffer : Disk IO를 최소화하기 위해 데이터를 캐싱하는 공간&lt;/li&gt;
&lt;li&gt;WAL buffer : &lt;strong&gt;Write Ahead Logging&lt;/strong&gt; 을 위한 공간으로 데이터베이스의 변경 사항을 WAL file로 내리기 전까지 보관&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;PostgreSQL은 데이터를 변경하기 전에 항상 변경 로그부터 기록하는데요&lt;br&gt;
Shared Buffer의 데이터를 변경하기 전에 먼저 WAL buffer에 로그를 기록하고,  writer process 가 dirty block 을 disk로 내려쓰기 전에&lt;br&gt;
WAL writer process가 WAL logfile로 내려쓰는 작업을 먼저 하는데 변경 로그부터 작업한다하여 이를 &lt;strong&gt;Write Ahead Logging&lt;/strong&gt; 이라고 부릅니다.&lt;/p&gt;
&lt;p&gt;Shared Memory에서 가장 중요한 공간인만큼 관련 parameter의 적절한 설정도 중요한데요&lt;br&gt;
권장값과 관련 설정은 이전 글을 참고하시기 바랍니다.&lt;br&gt;
&lt;a href=&#34;https://kimdubi.github.io/postgresql/psql_conf/&#34;&gt;https://kimdubi.github.io/postgresql/psql_conf/&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&#34;postgresql-process&#34;&gt;&lt;strong&gt;PostgreSQL Process&lt;/strong&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Postmaster Process&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Shared Memory 위에 보이는 &lt;strong&gt;Postmaster process&lt;/strong&gt; 는 PostgreSQL을 기동할 때 가장 먼저 시작되는 Daemon 프로세스로&lt;br&gt;
DB 기동 시에 recovery 작업, Shared Memory 초기화 작업 그리고 다른 background process 구동 작업을 수행하는 역할을 합니다.&lt;br&gt;
또한 클라이언트 프로세스(JDBC,ODBC등..)의 접속 요청이 있을 때 backend process 를 fork하여 client process와 연결시켜주는 역할을 합니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Background Process&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;운영에 필요한 &lt;strong&gt;PostgreSQL background process&lt;/strong&gt;는 아래와 같습니다.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Process 명&lt;/th&gt;
&lt;th&gt;역할&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;writer&lt;/td&gt;
&lt;td&gt;주기적으로 Shared Buffers 내 dirty 버퍼를 data file로 내려씁니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;WAL writer&lt;/td&gt;
&lt;td&gt;WAL buffer 내용을 WAL file에 기록합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Checkpointer&lt;/td&gt;
&lt;td&gt;체크포인트 발생 시 dirty 버퍼를 data file에 기록합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Archiver&lt;/td&gt;
&lt;td&gt;Archive log 모드 일 때 WAL 파일을 지정된 archive 디렉토리에 복사합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Logging collector&lt;/td&gt;
&lt;td&gt;error 메시지를 log file에 기록합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Stats collector&lt;/td&gt;
&lt;td&gt;DBMS의 세션 정보, 테이블 통계 같은 정보를 수집합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Autovaccum&lt;/td&gt;
&lt;td&gt;vacuum이 필요한 시점에 autovacuum worker를 fork하여 vacuum 작업을 수행합니다&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;!-- raw HTML omitted --&gt;
&lt;p&gt;vacuum 을 이해하기 위해서는 PostgreSQL의 MVCC 모델 구현방식에 대한 이해가 필요합니다.&lt;br&gt;
vacuum에 대해서는 PostgreSQL MVCC 모델에 대해 말씀드리며 간단히 설명드리도록 하겠습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Backend Process&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Backend process&lt;/strong&gt; 는 client process 의 쿼리 요청을 수행하고 그 결과를 전송하는 프로세스입니다.&lt;br&gt;
이 때 쿼리 수행에 필요한 메모리가 필요한데 이를 backend process의 &lt;strong&gt;local memory&lt;/strong&gt;라고 부릅니다.&lt;br&gt;
Shared Memory와 마찬가지로 적절한 parameter 튜닝을 통해 성능을 최적화하는 것이 중요합니다.&lt;/p&gt;
&lt;p&gt;여기까지 PostgreSQL의 Architecture에 대해 살펴보았는데 어떠신가요?&lt;br&gt;
개인적으로는 ORACLE의 아키텍처와 유사해서 친숙하기도 했고 혹시 차이점은 없읕까 더 궁금하기도 했습니다.&lt;br&gt;
다음으로는 제가 생각하는 ORACLE과 PostgreSQL의 가장 큰 차이점을 살펴보며 PostgreSQL Architecture에 대해 조금 더 알아보도록 하겠습니다.&lt;/p&gt;
&lt;h2 id=&#34;oracle-postgresql-architecture-&#34;&gt;ORACLE과 PostgreSQL Architecture 차이점&lt;/h2&gt;
&lt;h4 id=&#34;mvcc--&#34;&gt;&lt;strong&gt;MVCC 모델 구현&lt;/strong&gt;&lt;/h4&gt;
&lt;p&gt;대다수의 RDBMS는 동시성을 위해 MVCC(Multi-Version Concurrency Control) 기능을 제공합니다.&lt;br&gt;
MVCC란, 쿼리 수행 시점의 데이터를 제공하는 기법으로 기본 원리는 쿼리가 시작된 시점의 트랜잭션ID와 같거나 작은 데이터 버전을 읽는 것 입니다.&lt;/p&gt;
&lt;p&gt;ORACLE과 MySQL 같은 경우에는 &lt;strong&gt;UNDO segment&lt;/strong&gt;를 사용하며 쿼리가 시작된 이후에&lt;br&gt;
다른 트랜잭션에 의해 변경된 블록을 만나면 원본 블록으로부터 복사본 블록 (CR copy)를 만들고&lt;br&gt;
그 복사본 블록에 UNDO segment를 적용하여 쿼리가 시작된 시점으로 되돌려서 읽는 방식을 사용합니다.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_0808_3.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;[ORACLE 의 UNDO 동작방식]&lt;/p&gt;
&lt;p&gt;반면에 PostgreSQL은 &lt;strong&gt;데이터 페이지 내에 변경되기 이전 레코드를 저장&lt;/strong&gt; 하고&lt;br&gt;
레코드 별로 생성된 시점과 변경된 시점을 기록 및 비교하는 방식 으로 MVCC를 제공합니다.&lt;br&gt;
이 방식은 ORACLE의 UNDO segment 같은 복잡성 없이 MVCC를 매우 단순하게 구현할 수 있도록 하는 장점이 있습니다.&lt;/p&gt;
&lt;p&gt;다만 이전 데이터와 현재의 데이터를 동일한 테이블 내에 저장하기 때문에 &lt;strong&gt;테이블 공간이 비효율적&lt;/strong&gt;으로 사용될 수 있습니다.&lt;br&gt;
또한 사용할 수 있는 트랜잭션 ID가 고갈되고 덮어써져 (wrap around) &lt;strong&gt;오래된 데이터가 손실될 가능성&lt;/strong&gt;이 있습니다.&lt;/p&gt;
&lt;p&gt;PostgreSQL에서는 이전의 데이터와 현재의 데이터를 구분하고 시점에 맞는 데이터를 사용하기 위해서는 레코드별로 트랜잭션ID (이하 XID)가 필요한데&lt;br&gt;
PostgreSQL에서는 XID를 위해 4바이트를 사용하고 있어 최대 43억건의 XID를 표현할수 있습니다.&lt;br&gt;
만약 43억건 이후 XID 를 1부터 다시 시작하게 되면 어떻게 될까요?&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_0808_4.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;p&gt;아래의 새로 시작된 XID 1은 43억보다 이후의 트랜잭션이지만&lt;br&gt;
XID가 작아서 더 오래된 트랜잭션으로 인식되어 순서가 꼬여버리는 치명적인 문제가 발생할 수 있습니다.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;불필요한 데이터로 인한 테이블 공간 사용의 비효율성&lt;/li&gt;
&lt;li&gt;XID를 4bytes 로 관리함에 따른 XID 고갈&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;이 두가지 이슈를 해결하기 위한 PostgreSQL의 내부 동작이 &lt;strong&gt;vacuum&lt;/strong&gt; 입니다.&lt;br&gt;
위에서 언급한 &lt;strong&gt;autovacuum&lt;/strong&gt; 백그라운드 프로세스는 테이블에 일정량 이상의 변경이 발생한 경우&lt;br&gt;
자동으로 vacuum을 수행하면서 더 이상 사용하지 않는 dead-rows 를 회수하고 오래된 XID 를 정리하는 역할을 수행합니다.&lt;/p&gt;
&lt;p&gt;즉 vacuum은 PostgreSQL 특유의 MVCC 모델에서 비롯될 수 있는 문제점을 해결하기 위해 나온 개념인 것입니다.&lt;/p&gt;
&lt;h4 id=&#34;shared-pool-&#34;&gt;&lt;strong&gt;Shared Pool의 부재&lt;/strong&gt;&lt;/h4&gt;
&lt;p&gt;PostgreSQL은 ORACLE의 Shared Pool같은 기능을 제공하지 않습니다.&lt;br&gt;
Shared pool은 수행한 쿼리의 Parse tree나 Execution plan, 데이터베이스 내 오브젝트 정보를 갖고 있는 공간으로&lt;br&gt;
ORACLE에서는 굉장히 중요한 구성요소이기 때문에 개인적으로 놀라운 부분이기도 했는데요.&lt;/p&gt;
&lt;p&gt;PostgreSQL은 공유 메모리 레벨이 아닌 프로세스 레벨에서 SQL 정보를 공유합니다.&lt;br&gt;
이를 통해 Shared pool 리소스나 Shared pool latch 경합 같은 관리 포인트를 줄일 수 있지만 신규 커넥션 마다 수행하는 SQL에 대해 최초 1회는 hard-parsing 이 필요하게 됩니다.&lt;br&gt;
그렇기 때문에 Connection pool 기능을 사용하면 도움이 되겠죠?&lt;/p&gt;
&lt;p&gt;Pgpool-II , PGbouncer 같이 connection pooling, load balancing 등을 지원하는 PostgreSQL 오픈소스 툴을 도입해서 사용하면 더 좋은 성능을 이끌어 낼 수 있습니다.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL 유용한 command</title>
      <link>https://kimdubi.github.io/postgresql/command/</link>
      <pubDate>Sat, 08 Aug 2020 23:06:27 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/command/</guid>
      <description>&lt;p&gt;새로운 dbms를 공부할 때 가장 불편한 것 중에 하나는 ORACLE의 dba_ , v$ 테이블 같은 메타데이터 조회나 mysql의 &amp;ldquo;show tables&amp;rdquo; 같은&lt;br&gt;
유용하고 편리한 쿼리들을 어떻게 사용하는지 모른다는 것입니다.&lt;br&gt;
postgresql 에도 물론 메타데이터 조회나 쉽고 편한 명령어들이 많은데 이번 글에서는 그 커맨드들을 확인해보겠습니다.&lt;/p&gt;
&lt;h3 id=&#34;db---&#34;&gt;DB 접속 &amp;amp; 입출력&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ psql -d DBNAME
$ psql -d DBNAME -U USERNAME
=&amp;gt; OS에서 DB 접속


\c DBNAME
\c DBNAME USER 
\c DBNAME USER HOST 
\c DBNAME USER HOST PORT
=&amp;gt; DB내에서 database, user switching

\conninfo : 현재 커넥션의 정보 표시
\encoding [ENCODING] : 클라이언트의 인코딩을 셋팅 또는 표시

\timing 소요시간 표시

\p : 쿼리 버퍼 조회
\r : 쿼리 버퍼 리셋
\s : 이력 조회
\g : 버퍼의 쿼리 수행
\w : 쿼리 버퍼를 파일에 쓰기
\i : 파일 수행
\o [파일명] : 결과를 파일로 저장
\qecho : \o로 열었던 파일에 echo 수행
\o : 파일 닫기 (다시 결과를 standard output 으로 전달)

\set 변수 설정
\unset 변수 설정 해제
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;date 조회
postgres=# select now();
now&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;p&gt;2020-08-08 13:35:52.006784+00
(1 row)&lt;/p&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;쿼리 버퍼 조회 =&amp;gt; 바로 전에 수행한 쿼리만 남는듯
postgres=# \p
select now();&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;해당 세션 쿼리 수행 히스토리
postgres=# \s
\l
\dn
\dn+
\dt&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;재수행
postgres=# \g
now&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;p&gt;2020-08-08 13:38:23.966627+00
(1 row)&lt;/p&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;query buffer 내 쿼리 파일로 떨구기
postgres=# \w test.sql&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;쿼리 확인
postgres=# ! cat test.sql
select now();
#############################################&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;query file Input&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;now&#34;&gt;postgres-# \i test.sql
now&lt;/h2&gt;
&lt;p&gt;2020-08-08 13:41:23.455211+00
(1 row)&lt;/p&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;수행한 쿼리 결과를 파일로 떨구기&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;postgres=# \o test.txt
postgres=# \qecho ##### OUTPUT TEST #####
postgres=# select now();
postgres=# ! cat test.txt&lt;/p&gt;
&lt;h5 id=&#34;output-test&#34;&gt;OUTPUT TEST&lt;/h5&gt;
&lt;pre&gt;&lt;code&gt;          now
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;p&gt;2020-08-08 13:43:43.644294+00
(1 row)
postgres=# \o&lt;/p&gt;
&lt;p&gt;#############################################&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;변수 설정
postgres=# \set test 10
postgres=# select :test;
?column?&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;pre&gt;&lt;code&gt;   10
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;(1 row)&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;
### DB 내 테이블 ,인덱스 같은 오브젝트들을 조회하는 방법

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;\l[+]   [PATTERN]      list databases&lt;/p&gt;
&lt;p&gt;\d[S+]                 list tables, views, and sequences
\d[S+]  NAME           describe table, view, sequence, or index&lt;/p&gt;
&lt;p&gt;\du[S+] [PATTERN]      list roles
\dx[+]  [PATTERN]      list extensions
\dn[S+] [PATTERN]      list schemas
\db[+]  [PATTERN]      list tablespaces&lt;/p&gt;
&lt;p&gt;\dt[S+] [PATTERN]      list tables
\ds[S+] [PATTERN]      list sequences
\di[S+] [PATTERN]      list indexes
\dv[S+] [PATTERN]      list views
\dm[S+] [PATTERN]      list materialized views
\dy     [PATTERN]      list event triggers&lt;/p&gt;
&lt;h3 id=&#34;system-table--s-&#34;&gt;system table들을 조회하려면 S+를 붙여야함&lt;/h3&gt;
&lt;p&gt;postgres=# \d
List of relations
Schema | Name | Type  |  Owner
&amp;mdash;&amp;mdash;&amp;ndash;+&amp;mdash;&amp;mdash;+&amp;mdash;&amp;mdash;-+&amp;mdash;&amp;mdash;&amp;mdash;-
public | test | table | postgres
(1 row)&lt;/p&gt;
&lt;p&gt;=&amp;gt; 일반 service 테이블만 조회됨&lt;/p&gt;
&lt;p&gt;postgres=# \dS+
List of relations
Schema   |              Name               | Type  |  Owner   |    Size    | Description
&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;+&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;+&amp;mdash;&amp;mdash;-+&amp;mdash;&amp;mdash;&amp;mdash;-+&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;+&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;-
pg_catalog | pg_aggregate                    | table | postgres | 56 kB      |
pg_catalog | pg_am                           | table | postgres | 40 kB      |
pg_catalog | pg_amop                         | table | postgres | 80 kB      |
pg_catalog | pg_amproc                       | table | postgres | 56 kB      |
pg_catalog | pg_attrdef                      | table | postgres | 8192 bytes |
.
.
.
=&amp;gt; 시스템 테이블도 조회하려면 S or S+ 를 붙여야함&lt;/p&gt;
&lt;p&gt;postgres=# \dn
List of schemas
Name  |  Owner
&amp;mdash;&amp;mdash;&amp;ndash;+&amp;mdash;&amp;mdash;&amp;mdash;-
public | postgres
(1 row)&lt;/p&gt;
&lt;p&gt;postgres=# \dnS
List of schemas
Name        |  Owner
&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;mdash;&amp;ndash;+&amp;mdash;&amp;mdash;&amp;mdash;-
information_schema | postgres
pg_catalog         | postgres
pg_temp_1          | postgres
pg_toast           | postgres
pg_toast_temp_1    | postgres
public             | postgres
(6 rows)&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;

### 전체 커맨드
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;postgres-# ?&lt;/p&gt;
&lt;p&gt;General
\copyright             show PostgreSQL usage and distribution terms
\crosstabview [COLUMNS] execute query and display results in crosstab
\errverbose            show most recent error message at maximum verbosity
\g [FILE] or ;         execute query (and send results to file or |pipe)
\gdesc                 describe result of query, without executing it
\gexec                 execute query, then execute each value in its result
\gset [PREFIX]         execute query and store results in psql variables
\gx [FILE]             as \g, but forces expanded output mode
\q                     quit psql
\watch [SEC]           execute query every SEC seconds
Help
? [commands]          show help on backslash commands
? options             show help on psql command-line options
? variables           show help on special variables
\h [NAME]              help on syntax of SQL commands, * for all commands
Query Buffer
\e [FILE] [LINE]       edit the query buffer (or file) with external editor
\ef [FUNCNAME [LINE]]  edit function definition with external editor
\ev [VIEWNAME [LINE]]  edit view definition with external editor
\p                     show the contents of the query buffer
\r                     reset (clear) the query buffer
\s [FILE]              display history or save it to file
\w FILE                write query buffer to file
Input/Output
\copy &amp;hellip;              perform SQL COPY with data stream to the client host
\echo [STRING]         write string to standard output
\i FILE                execute commands from file
\ir FILE               as \i, but relative to location of current script
\o [FILE]              send all query results to file or |pipe
\qecho [STRING]        write string to query output stream (see \o)
Conditional
\if EXPR               begin conditional block
\elif EXPR             alternative within current conditional block
\else                  final alternative within current conditional block
\endif                 end conditional block
Informational
(options: S = show system objects, + = additional detail)
\d[S+]                 list tables, views, and sequences
\d[S+]  NAME           describe table, view, sequence, or index
\da[S]  [PATTERN]      list aggregates
\dA[+]  [PATTERN]      list access methods
\db[+]  [PATTERN]      list tablespaces
\dc[S+] [PATTERN]      list conversions
\dC[+]  [PATTERN]      list casts
\dd[S]  [PATTERN]      show object descriptions not displayed elsewhere
\dD[S+] [PATTERN]      list domains
\ddp    [PATTERN]      list default privileges
\dE[S+] [PATTERN]      list foreign tables
\det[+] [PATTERN]      list foreign tables
\des[+] [PATTERN]      list foreign servers
\deu[+] [PATTERN]      list user mappings
\dew[+] [PATTERN]      list foreign-data wrappers
\df[anptw][S+] [PATRN] list [only agg/normal/procedures/trigger/window] functions
\dF[+]  [PATTERN]      list text search configurations
\dFd[+] [PATTERN]      list text search dictionaries
\dFp[+] [PATTERN]      list text search parsers
\dFt[+] [PATTERN]      list text search templates
\dg[S+] [PATTERN]      list roles
\di[S+] [PATTERN]      list indexes
\dl                    list large objects, same as \lo_list
\dL[S+] [PATTERN]      list procedural languages
\dm[S+] [PATTERN]      list materialized views
\dn[S+] [PATTERN]      list schemas
\do[S]  [PATTERN]      list operators
\dO[S+] [PATTERN]      list collations
\dp     [PATTERN]      list table, view, and sequence access privileges
\drds [PATRN1 [PATRN2]] list per-database role settings
\dRp[+] [PATTERN]      list replication publications
\dRs[+] [PATTERN]      list replication subscriptions
\ds[S+] [PATTERN]      list sequences
\dt[S+] [PATTERN]      list tables
\dT[S+] [PATTERN]      list data types
\du[S+] [PATTERN]      list roles
\dv[S+] [PATTERN]      list views
\dx[+]  [PATTERN]      list extensions
\dy     [PATTERN]      list event triggers
\l[+]   [PATTERN]      list databases
\sf[+]  FUNCNAME       show a function&#39;s definition
\sv[+]  VIEWNAME       show a view&#39;s definition
\z      [PATTERN]      same as \dp
Formatting
\a                     toggle between unaligned and aligned output mode
\C [STRING]            set table title, or unset if none
\f [STRING]            show or set field separator for unaligned query output
\H                     toggle HTML output mode (currently off)
\pset [NAME [VALUE]]   set table output option
(NAME := {border|columns|expanded|fieldsep|fieldsep_zero|
footer|format|linestyle|null|numericlocale|pager|
pager_min_lines|recordsep|recordsep_zero|tableattr|title|
tuples_only|unicode_border_linestyle|
unicode_column_linestyle|unicode_header_linestyle})
\t [on|off]            show only rows (currently off)
\T [STRING]            set HTML &lt;!-- raw HTML omitted --&gt; tag attributes, or unset if none
\x [on|off|auto]       toggle expanded output (currently off)
Connection
\c[onnect] {[DBNAME|- USER|- HOST|- PORT|-] | conninfo}
connect to new database (currently &amp;ldquo;naverdb&amp;rdquo;)
\conninfo              display information about current connection
\encoding [ENCODING]   show or set client encoding
\password [USERNAME]   securely change the password for a user
Operating System
\cd [DIR]              change the current working directory
\setenv NAME [VALUE]   set or unset environment variable
\timing [on|off]       toggle timing of commands (currently on)
! [COMMAND]           execute command in shell or start interactive shell
Variables
\prompt [TEXT] NAME    prompt user to set internal variable
\set [NAME [VALUE]]    set internal variable, or list all if no parameters
\unset NAME            unset (delete) internal variable
Large Objects
\lo_export LOBOID FILE
\lo_import FILE [COMMENT]
\lo_list
\lo_unlink LOBOID      large object operations&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;


&lt;/code&gt;&lt;/pre&gt;</description>
    </item>
    
    <item>
      <title>PostgreSQL migration tool pgloader 사용법</title>
      <link>https://kimdubi.github.io/postgresql/pgloader/</link>
      <pubDate>Tue, 07 Jul 2020 14:15:08 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/pgloader/</guid>
      <description>&lt;h3 id=&#34;pgloader&#34;&gt;pgloader란?&lt;/h3&gt;
&lt;p&gt;csv 같은 File이나 실제 DB로부터 데이터를 읽어와 target postgresql 로 데이터 migration을 지원해주는 툴&lt;br&gt;
아래와 같은 단계로 source DB로 부터 데이터를 읽어와 target DB로 copy 수행&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Fetch meta data and catalogs&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### table, column metadata

  select c.table_name, t.table_comment,
         c.column_name, c.column_comment,
         c.data_type, c.column_type, c.column_default,
         c.is_nullable, c.extra
    from information_schema.columns c
         join information_schema.tables t using(table_schema, table_name)
   where c.table_schema = &#39;source DB&#39; and t.table_type = &#39;BASE TABLE&#39;


### FK 제약조건
SELECT s.table_name, s.constraint_name, s.ft, s.cols, s.fcols,
       rc.update_rule, rc.delete_rule
FROM
 (
  SELECT tc.table_schema, tc.table_name,
         tc.constraint_name, k.referenced_table_name ft,
             group_concat(         k.column_name
                          order by k.ordinal_position) as cols,
             group_concat(         k.referenced_column_name
                          order by k.position_in_unique_constraint) as fcols
        FROM information_schema.table_constraints tc
        LEFT JOIN information_schema.key_column_usage k
               ON k.table_schema = tc.table_schema
              AND k.table_name = tc.table_name
              AND k.constraint_name = tc.constraint_name
      WHERE     tc.table_schema = &#39;source DB&#39;
            AND k.referenced_table_schema = &#39;source DB&#39;
            AND tc.constraint_type = &#39;FOREIGN KEY&#39;
   GROUP BY tc.table_schema, tc.table_name, tc.constraint_name, ft
 ) s
             JOIN information_schema.referential_constraints rc
               ON rc.constraint_schema = s.table_schema
              AND rc.constraint_name = s.constraint_name
              AND rc.table_name = s.table_name
              
### index 구성
  SELECT table_name, index_name, index_type,
         sum(non_unique),
         cast(GROUP_CONCAT(column_name order by seq_in_index) as char)
    FROM information_schema.statistics
   WHERE table_schema = &#39;source DB&#39;
GROUP BY table_name, index_name, index_type

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; source DB(MySQL) 의 catalog를 쿼리하여 가져오는 단계&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Prepare the target PostgreSQL database&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;source DB의 catalog를 target postgresql의 catalog로 변환 후 schema를 생성하고
casting rule 이 있다면 정의된 casting rule을 적용함 (column type 이나 default value 같은 설정)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;data copy&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;source db에서 data를 select하는 reader Threads 와
target db로 data를 write하는 writer threads 로 나뉘어 migration 작업을 수행함
parallel로 수행 가능
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;Create the indexes in parallel mode&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;object copy 후 index 생성하는 단계, parallel로 수행됨
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;제약조건 적용 및 pgloader 종료&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;PK, FK 제약조건 적용 후 migration 종료
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;pgloader-&#34;&gt;pgloader 설치&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;yum install -y sbcl git curl patch unzip devscripts pandoc libsqlite3-dev freetds-dev

wget https://github.com/dimitri/pgloader/archive/v3.6.1.tar.gz
tar xvf v3.6.1.tar.gz
cd pgloader-3.6.1/
make pgloader

sudo mv ./build/bin/pgloader /usr/local/bin/

pgloader --version
pgloader version &amp;quot;3.6.2&amp;quot;
compiled with SBCL 1.3.6
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;sbcl은 pgloader 3.6.1, centos6 기준 sbcl 1.3.6 이상 설치해야함!!&lt;/li&gt;
&lt;li&gt;internet 연결안된 private 환경에서는 bundle 파일로 설치
&lt;a href=&#34;https://github.com/dimitri/pgloader/releases&#34;&gt;https://github.com/dimitri/pgloader/releases&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;source-mysql-table-schema&#34;&gt;source MySQL table schema&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE TABLE `place` (
  `place_seq` bigint(20) NOT NULL AUTO_INCREMENT,
  `place_type_code` varchar(20) NOT NULL,
  `nation_code` char(2) NOT NULL DEFAULT &#39;KR&#39;,
  `name` varchar(200) NOT NULL,
  `description` varchar(1000) DEFAULT NULL,
  `latitude` decimal(11,8) NOT NULL,
  `longitude` decimal(11,8) NOT NULL,
  `area_info_seq` bigint(20) NOT NULL,
  `region_sido_area_info_seq` int(11) NOT NULL DEFAULT &#39;0&#39;,
  `region_sigungu_area_info_seq` int(11) NOT NULL DEFAULT &#39;0&#39;,
  `region_upmyundong_area_info_seq` int(11) NOT NULL DEFAULT &#39;0&#39;,
  `address_detail` varchar(400) NOT NULL,
  `postcode` char(5) NOT NULL,
  `homepage_url` varchar(1000) DEFAULT NULL,
  `sns_urls` json DEFAULT NULL,
  `telephone` varchar(30) DEFAULT NULL,
  `telephone_display` varchar(30) DEFAULT NULL,
  `main_image_url` varchar(1000) DEFAULT NULL,
  `additional_info` varchar(1000) DEFAULT NULL,
  `min_price` int(11) NOT NULL DEFAULT &#39;0&#39;,
  `max_price` int(11) NOT NULL DEFAULT &#39;0&#39;,
  `ad_yn` char(1) NOT NULL DEFAULT &#39;N&#39;,
  `partner_yn` char(1) NOT NULL DEFAULT &#39;N&#39;,
  `search_yn` char(1) NOT NULL DEFAULT &#39;Y&#39;,
  `delete_yn` char(1) NOT NULL DEFAULT &#39;N&#39;,
  `source_info` varchar(100) DEFAULT NULL,
  `load_ymdt` datetime DEFAULT NULL,
  `search_indexing_ymdt` datetime DEFAULT NULL,
  `register_ymdt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `modify_ymdt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`place_seq`)
) ENGINE=InnoDB AUTO_INCREMENT=293057 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;migtraion-test&#34;&gt;migtraion test&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;prepare&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### postgre schema 생성
CREATE SCHEMA postgre authorization testuser;
=&amp;gt;MySQL source DB가 postgresql 의 schema로 migration 되기 때문에 사전에 생성해둠

### search_path 설정
* DB level
ALTER DATABASE &amp;quot;testdb&amp;quot; SET search_path TO testuser, postgre;
* session level
set search_path=testuser,postgre;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pgloader 수행&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;pgloader mysql://[user[:password]@][netloc][:port][/dbname][?option=value&amp;amp;&amp;hellip;]  postgresql://[user[:password]@][netloc][:port][/dbname][?option=value&amp;amp;&amp;hellip;]&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[irteam@testserver2 15:11:26 ~/psql/pgloader-bundle-3.6.2/bin ]$ pgloader mysql://mig_test:&#39;qhdks123&#39;@11.111.11.11:13306/postgre postgresql://testuser:&#39;&#39;@22.222.22.22:3000/testdb
2020-05-30T15:16:58.031000+09:00 LOG pgloader version &amp;quot;3.6.2&amp;quot;
2020-05-30T15:16:58.119000+09:00 LOG Migrating from #&amp;lt;MYSQL-CONNECTION mysql://mig_test@11.111.11.11:13306/postgre {100892E793}&amp;gt;
2020-05-30T15:16:58.119000+09:00 LOG Migrating into #&amp;lt;PGSQL-CONNECTION pgsql://testuser@22.222.22.22:3000/testdb {1008B80E03}&amp;gt;


2020-05-30T15:17:04.811000+09:00 ERROR PostgreSQL Database error 42501: must be owner of database testdb
QUERY: ALTER DATABASE &amp;quot;testdb&amp;quot; SET search_path TO testuser, postgre;
2020-05-30T15:17:04.815000+09:00 LOG report summary reset
                     table name     errors       rows      bytes      total time
-------------------------------  ---------  ---------  ---------  --------------
                fetch meta data          0          7                     0.087s
                 Create Schemas          0          0                     0.000s
               Create SQL Types          0          0                     0.007s
                  Create tables          0          6                     0.031s
                 Set Table OIDs          0          3                     0.005s
-------------------------------  ---------  ---------  ---------  --------------
postgre.mobile_verification_log          0          0                     0.335s
                  postgre.place          0     225127    69.7 MB          6.255s
postgre.place_category_mappings          0     412454    19.7 MB          2.232s
-------------------------------  ---------  ---------  ---------  --------------
        COPY Threads Completion          0          4                     6.254s
                 Create Indexes          0          4                     0.589s
         Index Build Completion          0          4                     0.119s
                Reset Sequences          0          2                     0.041s
                   Primary Keys          0          3                     0.003s
            Create Foreign Keys          0          0                     0.000s
                Create Triggers          0          0                     0.000s
                Set Search Path          1          0                     0.001s
               Install Comments          0          0                     0.000s
-------------------------------  ---------  ---------  ---------  --------------
              Total import time          ?     637581    89.4 MB          7.007s

&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pgloader 결과&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;testdb=&amp;gt; \dt
                  List of relations
 Schema  |          Name           | Type  |  Owner
---------+-------------------------+-------+----------
 postgre | place                   | table | testuser
 postgre | place_category_mappings | table | testuser
(2 rows)

testdb=&amp;gt; select count(*) from place;
 count
--------
 225127
(1 row)

testdb=&amp;gt; select count(*) from place_category_mappings;
 count
--------
 412454
(1 row)


testdb=&amp;gt; \d place
                                                    Table &amp;quot;postgre.place&amp;quot;
             Column              |           Type           | Collation | Nullable |                 Default
---------------------------------+--------------------------+-----------+----------+------------------------------------------
 place_seq                       | bigint                   |           | not null | nextval(&#39;place_place_seq_seq&#39;::regclass)
 place_type_code                 | character varying(20)    |           | not null |
 nation_code                     | character(2)             |           | not null | &#39;KR&#39;::bpchar
 name                            | character varying(200)   |           | not null |
 description                     | character varying(1000)  |           |          |
 latitude                        | numeric(11,8)            |           | not null |
 longitude                       | numeric(11,8)            |           | not null |
 area_info_seq                   | bigint                   |           | not null |
 region_sido_area_info_seq       | bigint                   |           | not null | &#39;0&#39;::bigint
 region_sigungu_area_info_seq    | bigint                   |           | not null | &#39;0&#39;::bigint
 region_upmyundong_area_info_seq | bigint                   |           | not null | &#39;0&#39;::bigint
 address_detail                  | character varying(400)   |           | not null |
 postcode                        | character(5)             |           | not null |
 homepage_url                    | character varying(1000)  |           |          |
 sns_urls                        | json                     |           |          |
 telephone                       | character varying(30)    |           |          |
 telephone_display               | character varying(30)    |           |          |
 main_image_url                  | character varying(1000)  |           |          |
 additional_info                 | character varying(1000)  |           |          |
 min_price                       | bigint                   |           | not null | &#39;0&#39;::bigint
 max_price                       | bigint                   |           | not null | &#39;0&#39;::bigint
 ad_yn                           | character(1)             |           | not null | &#39;N&#39;::bpchar
 partner_yn                      | character(1)             |           | not null | &#39;N&#39;::bpchar
 search_yn                       | character(1)             |           | not null | &#39;Y&#39;::bpchar
 delete_yn                       | character(1)             |           | not null | &#39;N&#39;::bpchar
 source_info                     | character varying(100)   |           |          |
 load_ymdt                       | timestamp with time zone |           |          |
 search_indexing_ymdt            | timestamp with time zone |           |          |
 register_ymdt                   | timestamp with time zone |           | not null | CURRENT_TIMESTAMP
 modify_ymdt                     | timestamp with time zone |           | not null | CURRENT_TIMESTAMP
Indexes:
    &amp;quot;idx_49946_primary&amp;quot; PRIMARY KEY, btree (place_seq)

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; 데이터 건 수 , schema 모두 정상적으로 이관 됨&lt;/p&gt;
&lt;h3 id=&#34;pgloader---&#34;&gt;pgloader 옵션 주면서 수행하기&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;ORACLE parfile 처럼 export, import 설정을 파일에 저장해놓고 사용할 수 있음&lt;/li&gt;
&lt;li&gt;Source / Target DB 에 대한 설정, pgloader 작업 전 / 후 의 작업도 설정가능함&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;$ vi test.load

LOAD DATABASE
  FROM mysql://mig_test:&#39;qhdks123&#39;@11.111.11.11:13306/postgre
  INTO postgresql://testuser:&#39;&#39;@22.222.22.22:3000/testdb

  WITH on error stop, include drop, batch rows=10000, workers = 4, concurrency = 1,
       multiple readers per thread,
       max parallel create index =4 
  SET MySQL PARAMETERS
      net_read_timeout  = &#39;120&#39;,
      net_write_timeout = &#39;120&#39;

  BEFORE LOAD DO
    $$ create schema if not exists testuser; $$

  AFTER LOAD DO
    $$ set search_path=testuser,postgre; $$
;


[irteam@testserver2 00:53:38 ~/psql/pgloader-bundle-3.6.2/bin ]$ pgloader test.load

2020-05-31T00:53:42.044000+09:00 LOG pgloader version &amp;quot;3.6.2&amp;quot;
2020-05-31T00:53:42.178000+09:00 LOG Migrating from #&amp;lt;MYSQL-CONNECTION mysql://mig_test@11.111.11.11:13306/postgre {1009750FE3}&amp;gt;
2020-05-31T00:53:42.178000+09:00 LOG Migrating into #&amp;lt;PGSQL-CONNECTION pgsql://testuser@22.222.22.22:3000/testdb {10097523F3}&amp;gt;
2020-05-31T00:53:48.688000+09:00 LOG report summary reset
                     table name     errors       rows      bytes      total time
-------------------------------  ---------  ---------  ---------  --------------
                    before load          0          1                     0.017s
                fetch meta data          0          7                     0.086s
                 Create Schemas          0          0                     0.001s
               Create SQL Types          0          0                     0.006s
                  Create tables          0          6                     0.035s
                 Set Table OIDs          0          3                     0.005s
-------------------------------  ---------  ---------  ---------  --------------
postgre.mobile_verification_log          0          0                     0.118s
                  postgre.place          0     225127    69.7 MB          6.089s
postgre.place_category_mappings          0     412454    19.7 MB          2.234s
-------------------------------  ---------  ---------  ---------  --------------
        COPY Threads Completion          0          4                     6.084s
                 Create Indexes          0          4                     0.485s
         Index Build Completion          0          4                     0.108s
                Reset Sequences          0          2                     0.023s
                   Primary Keys          0          3                     0.003s
            Create Foreign Keys          0          0                     0.000s
                Create Triggers          0          0                     0.000s
               Install Comments          0          0                     0.000s
                     after load          0          1                     0.014s
-------------------------------  ---------  ---------  ---------  --------------
              Total import time          ?     637581    89.4 MB          6.717s
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://pgloader.readthedocs.io/en/latest/ref/mysql.html&#34;&gt;https://pgloader.readthedocs.io/en/latest/ref/mysql.html&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://pgloader.readthedocs.io/en/latest/pgloader.html&#34;&gt;https://pgloader.readthedocs.io/en/latest/pgloader.html&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL 백업 복구 테스트</title>
      <link>https://kimdubi.github.io/postgresql/psql_backup/</link>
      <pubDate>Tue, 07 Jul 2020 14:06:54 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_backup/</guid>
      <description>&lt;h2 id=&#34;pgdump&#34;&gt;pg_dump&lt;/h2&gt;
&lt;p&gt;PostgreSQL에서 기본 제공하는 logical backup 방식으로 mysqldump 와 같은 방법&lt;br&gt;
백업 수행 시점의 스냅샷만 가능하며 시점복구는 불가함&lt;br&gt;
pg_dump는 단일 DB backup, pg_dumpall은 전체 DB backup할 때 사용함&lt;/p&gt;
&lt;h3 id=&#34;backup-script&#34;&gt;backup script&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash
DEL_FILE=$(date -d &#39;2 day ago&#39; +&#39;%Y-%m-%d_&#39;)&amp;quot;*.dump&amp;quot;
FILENAME=$(date +&amp;quot;%Y-%m-%d_%H%M&amp;quot;).dump
BACKUP_DIR=/home1/irteam/psql/backup/testdb
PG_USER=&amp;quot;irteam&amp;quot;
PASSWORD=&amp;quot;&amp;quot;

cd $BACKUP_DIR
echo &amp;quot;DB backup start time : &amp;quot; $(date +&amp;quot;%Y-%m-%d %H:%M:%S&amp;quot;)
pg_dumpall -h localhost -p 3000 -U $PG_USER -v -j 3 -f &amp;quot;${BACKUP_DIR}/${FILENAME}&amp;quot; &amp;gt;&amp;amp; &amp;quot;${BACKUP_DIR}/${FILENAME}&amp;quot;.log?
echo &amp;quot;Successful db backup ( ${BACKUP_DIR}/DBNAME_${FILENAME} )&amp;quot;
echo &amp;quot;Delete old file DBNAME_${DEL_FILE}&amp;quot;
rm &amp;quot;${BACKUP_DIR}/DBNAME_${DEL_FILE}&amp;quot;
echo &amp;quot;BACKUP - End time : &amp;quot; $(date +&amp;quot;%Y-%m-%d %H:%M:%S&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;-d, &amp;ndash;dbname : Backup할 Database 명&lt;/li&gt;
&lt;li&gt;-h, &amp;ndash;host : Database 주소&lt;/li&gt;
&lt;li&gt;-U, &amp;ndash;username : Database 접속 시 User ID&lt;/li&gt;
&lt;li&gt;-f, &amp;ndash;file : Backup File Name&lt;/li&gt;
&lt;li&gt;-t, &amp;ndash;table : 특정 Table만 Backup하려할 때 대상이 되는 Table 명&lt;/li&gt;
&lt;li&gt;-j, &amp;ndash;jobs : Backup 시 병렬 처리&lt;/li&gt;
&lt;li&gt;-v, &amp;ndash;verbose : 진행 과정 표시
&lt;!-- raw HTML omitted --&gt;
&lt;!-- raw HTML omitted --&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;restore-test&#34;&gt;restore test&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;psql -p 3000 -d postgres -f 2020-04-10_1431.dump
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pg_dump 를 통한 백업은 pg_restore를 통해 복구함&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;pgstartbackup&#34;&gt;pg_start_backup&lt;/h2&gt;
&lt;p&gt;PostgreSQL 에서 제공하는 hot backup 기능, 백업 본 + WAL 로그 파일 replay 로 원하는 시점 복구 가능&lt;/p&gt;
&lt;h3 id=&#34;hotbackupsh&#34;&gt;hot_backup.sh&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;#!/bin/bash

### [set ENV.] ###########################################################
export PG_PORT=3000
export PG_DATA=/home1/irteam/psql/engn/PGSQL
export PG_ARCH=/home1/irteam/psql/arch/testdb
export PG_USER=irteam
export PG_DB=testdb

export BACKUP_DIR=/home1/irteam/psql/backup/testdb
##########################################################################

# Default ENV
export LANG=C
export CURRENT_DATE=`date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;`
export BULIST=${BACKUP_DIR}/LOG/.HOT-${PG_DB}-BULIST.dat
export TMPFILE=${BACKUP_DIR}/LOG/HOT-${PG_DB}.log
export LOGFILE=${BACKUP_DIR}/LOG/HOT-${PG_DB}-${CURRENT_DATE}.log

# SQL LIST
BACKUP_START=&amp;quot;SELECT pg_start_backup(&#39;full_backup&#39;);&amp;quot;
BACKUP_STOP=&amp;quot;SELECT pg_stop_backup();&amp;quot;

echo &amp;quot;########## [Step 1] `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` BEGIN BACKUP ##########&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}

psql -p ${PG_PORT} -d ${PG_DB} -U ${PG_USER} -c &amp;quot;${BACKUP_START}&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}  2&amp;gt;&amp;amp;1

if [ $? -eq &amp;quot;0&amp;quot; ]
then
  echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` Begin backup completed&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
else
  echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` Begin backup failed&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
  exit 1
fi

echo &amp;quot; &amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
echo &amp;quot;########## [Step 2] `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` DATA COPY ##########&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}

export TMP=${BACKUP_DIR}/LOG/.HOT-${PG_DB}.tmp

psql -p ${PG_PORT} -d ${PG_DB} -U ${PG_USER} &amp;lt;&amp;lt;! &amp;gt; ${TMP}
select distinct &#39;value&#39;, case when length(pg_tablespace_location(oid))=0 then (select setting from pg_settings where name=&#39;data_directory&#39;)
else pg_tablespace_location(oid) end
from pg_tablespace
where spcname not in (&#39;ts_temp&#39;); \q
!

/bin/cat ${TMP} | /bin/awk &#39;$1 == &amp;quot;value&amp;quot; { print $3 }&#39; &amp;gt; ${BULIST}

echo ${PG_ARCH} &amp;gt;&amp;gt; ${BULIST}
IFS=$&#39;\n&#39; read -d &#39;&#39; -r -a lines &amp;lt; ${BULIST}


echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; Backup List&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
cat ${BULIST} &amp;gt;&amp;gt; ${TMPFILE}
echo &amp;quot; &amp;quot; &amp;gt;&amp;gt; ${TMPFILE}

echo ${lines[*]}
#CHKPOINT=$(find /home1/irteam/psql/arch/testdb -type f -mtime +1 -name &#39;*backup&#39; -printf &#39;%f\n&#39; | sort -r | head -1)
tar -cv  --exclude=&amp;quot;${PG_DATA}/pg_wal/*&amp;quot; \-f $BACKUP_DIR/backup.tar ${lines[*]}

echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` data copy completed&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}

echo &amp;quot; &amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
echo &amp;quot;########## [Step 3] `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` END BACKUP ##########&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
psql -p ${PG_PORT} -d ${PG_DB} -U ${PG_USER} -c &amp;quot;${BACKUP_STOP}&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}  2&amp;gt;&amp;amp;1

if [ $? -eq &amp;quot;0&amp;quot; ]
then
  echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;`  Backup completed&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
  echo &amp;quot; &amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
#  psql -p ${PG_PORT} -d ${PG_DB} -U ${PG_USER} -c &amp;quot;select pg_switch_xlog();&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}  2&amp;gt;&amp;amp;1
#  psql -p ${PG_PORT} -d ${PG_DB} -U ${PG_USER} -c &amp;quot;select pg_switch_xlog();&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}  2&amp;gt;&amp;amp;1
  echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` HOT Backup completed successfully!!!&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
  mv ${TMPFILE} ${LOGFILE}
else
  echo &amp;quot;&amp;gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; `date &amp;quot;+%Y-%m-%d-%H:%M:%S&amp;quot;` HOT Backup failed!!!&amp;quot; &amp;gt;&amp;gt; ${TMPFILE}
  exit 1
fi

rm -rf ${BACKUP_DIR}/LOG/.*
find ${BACKUP_DIR}/LOG -name &amp;quot;HOT-*.log&amp;quot; -mtime +2 -exec rm {} \;
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;restore-test1&#34;&gt;restore test&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;데이터 삭제 전&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# select now(), count(*) from tb_test;
             now              | count
------------------------------+--------
 2020-04-13 00:08:07.88325+09 | 100000
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;backup 수행&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### hotbackup.sh 수행

### backup info 확인 
[irteam@testserver1 00:21:24 ~/psql/arch/testdb ]$ cat 000000020000000100000030.00000028.backup

START WAL LOCATION: 1/30000028 (file 000000020000000100000030)
STOP WAL LOCATION: 1/30006338 (file 000000020000000100000030)
CHECKPOINT LOCATION: 1/30006268
BACKUP METHOD: pg_start_backup
BACKUP FROM: master
START TIME: 2020-04-12 15:10:52 GMT
LABEL: full_backup
START TIMELINE: 2
STOP TIME: 2020-04-12 15:10:53 GMT
STOP TIMELINE: 2
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;START WAL LOCATION : 복구를 위한 시작점&lt;/li&gt;
&lt;li&gt;CHECKPOINT LOCATION : pg_start_backup를 실행했을 때 발생한 체크포인트가 기록된 LSN 위치&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# select pg_walfile_name (&#39;1/30006268&#39;);
     pg_walfile_name
--------------------------
 000000020000000100000031
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;BACKUP METHOD : 백업 방식에 대한 설명. pg_start_backup 또는 pg_basebackup 값을 갖는다&lt;/li&gt;
&lt;li&gt;BACKUP FROM : 백업이 primary와 standby 중에 어디에서 발생했는지 기록한다&lt;/li&gt;
&lt;li&gt;START TIME : pg_start_backup가 실행된 시간&lt;/li&gt;
&lt;li&gt;LABEL : pg_start_backup 실행시 입력한 라벨 값&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;백업 수행 시 백업 시작과 완료 시점의 WAL LOCATION 정보가 남기 때문에 복구 시 모든 WAL archive 파일을 읽을 필요없음 &lt;br&gt;
START WAL LOCATION 부터 recover_target_time 에 해당하는 WAL logfile 까지 필요함&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;data 삭제&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# call insert_test_data(10);
CALL

postgres=# select now(), count(*) from tb_test;
             now              | count
------------------------------+--------
 2020-04-13 00:12:54.00624+09 | 200000
(1 row)


postgres=# truncate public.tb_test;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt;truncate 직전 20만건으로 복구시도&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;backup 본으로 복구 수행&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### DB STOP

### backup 본 $PGDATA 로 copy
[irteam@testserver1 23:30:19 ~/psql/backup/testdb/home1/irteam/psql ]$ ls
arch  data  engn  indx  testdb


### $/PGDATA/recovery.conf 설정

cat recovery.conf

restore_command = &#39;cp /home1/irteam/psql/arch/testdb/%f %p&#39;
recovery_target_time = &#39;2020-04-13 00:12:54&#39;

==&amp;gt; wal archive logfile을 pg_wal 디렉토리로 가져오고 원하는 시간대로 복구하도록 설정  
DB 기동 시 recovery.conf 를 읽고 필요한 아카이브 파일을 적용하게됨  

### DB start


### recovery 완료
postgres=# select now(), count(*) from public.tb_test;
              now              | count
-------------------------------+--------
 2020-04-13 00:16:55.094237+09 | 200000
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;recovery log&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;2020-04-12 15:16:23 GMT LOG:  restored log file &amp;quot;00000002.history&amp;quot; from archive
2020-04-12 15:16:23 GMT LOG:  restored log file &amp;quot;000000020000000100000030&amp;quot; from archive

2020-04-12 15:16:23 GMT LOG:  redo starts at 1/30000028
2020-04-12 15:16:23 GMT LOG:  consistent recovery state reached at 1/30006338
=&amp;gt; 백업 진행 중 변경사항 적용

2020-04-12 15:16:23 GMT LOG:  database system is ready to accept read only connections
2020-04-12 15:16:24 GMT LOG:  restored log file &amp;quot;000000020000000100000031&amp;quot; from archive
2020-04-12 15:16:24 GMT LOG:  restored log file &amp;quot;000000020000000100000032&amp;quot; from archive
2020-04-12 15:16:24 GMT LOG:  recovery stopping before commit of transaction 1966, time 2020-04-13 00:13:19.690669+09
2020-04-12 15:16:24 GMT LOG:  recovery has paused
2020-04-12 15:16:24 GMT HINT:  Execute pg_wal_replay_resume() to continue.

=&amp;gt; 백업 이후 ~ recovery_target_time  사이의 데이터 반영  
archive 반영 후 데이터 복구는 완료 되어도 DB는 계속 recovery 상태 (read_only )이기 때문에 정상화 해야함
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;recovery 상태 해제&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# call insert_test_data(1);
ERROR:  cannot execute INSERT in a read-only transaction


postgres=# SELECT pg_wal_replay_resume();
 pg_wal_replay_resume
----------------------

(1 row)

postgres=# call insert_test_data(1);
CALL
&lt;/code&gt;&lt;/pre&gt;</description>
    </item>
    
    <item>
      <title>PostgreSQL pgpool을 활용한 auto-failover 구성</title>
      <link>https://kimdubi.github.io/postgresql/psql_pgpool/</link>
      <pubDate>Tue, 07 Jul 2020 11:36:34 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_pgpool/</guid>
      <description>&lt;h2 id=&#34;pgpoolii-&#34;&gt;Pgpool-II 이란?&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/pgpool.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Pgpool-II 는 PostgreSQL DB서버와 클라이언트 사이에 위치하는 proxy 개념의 미들웨어로 아래의 세가지 기능을 제공함&lt;/li&gt;
&lt;li&gt;connection pooling : DB서버와의 연결을 유지하고 동일한 속성(user,db,protocol) 을 가진 새로운 연결 요청이 오면 이를 재사용함&lt;/li&gt;
&lt;li&gt;load balancing : replication 을 사용하는 서비스에서 DB서버의 편중된 load를 줄이기 위해 SELECT 쿼리를 처리 가능한 서버 간 배포하는 기능&lt;/li&gt;
&lt;li&gt;automated failover : DB 서버 중 하나가 down되면 이를 차단하고 나머지 DB에서 서비스를 지속함, master down 될 시엔 다른 standby 서버를 승격시킴&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;pgpool-&#34;&gt;PGPool 구성방법&lt;/h2&gt;
&lt;h3 id=&#34;pgpool-1&#34;&gt;pgpool 설치&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;a href=&#34;https://pgpool.net/mediawiki/index.php/Main_Page&#34;&gt;https://pgpool.net/mediawiki/index.php/Main_Page&lt;/a&gt;
2020.04 기준 최신 stable 버전인 4.0 사용&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;yum&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;yum install http://www.pgpool.net/yum/rpms/4.0/redhat/rhel-6-x86_64/pgpool-II-release-4.0-1.noarch.rpm
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;yum 안될 때&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;yum install -y memcached
rpm -ivh pgpool-II-pg11-4.0.8-1pgdg.rhel6.x86_64.rpm
rpm -ivh pgpool-II-pg11-debuginfo-4.0.8-1pgdg.rhel6.x86_64.rpm
rpm -ivh pgpool-II-pg11-devel-4.0.8-1pgdg.rhel6.x86_64.rpm
rpm -ivh pgpool-II-pg11-extensions-4.0.8-1pgdg.rhel6.x86_64.rpm
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;pgpoolconf-&#34;&gt;pgpool.conf 설정&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vi /etc/pgpool-II/pgpool.conf&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;sudo cp /etc/pgpool-II/pgpool.conf.sample-stream pgpool.conf
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; pgpool.conf sample 파일을 이용하고 필요한 파라미터만 수정&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;conf 파일 수정 내용&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;...
#------------------------------------------------------------------------------
# CONNECTIONS
#------------------------------------------------------------------------------

# - pgpool Connection Settings -
listen_addresses = &#39;*&#39;
port = 4000
socket_dir = &#39;/tmp/.s.PGSQL.3000&#39;
...
# - Backend Connection Settings -
backend_hostname0 = &#39;PRIMARY IP address&#39;
backend_port0 = 3000
backend_weight0 = 1
backend_data_directory0 = &#39;/home1/testuser/psql/engn/PGSQL&#39;
backend_flag0 = &#39;ALLOW_TO_FAILOVER&#39;

backend_hostname1 = &#39;STANDBY IP address&#39;
backend_port1 = 3000
backend_weight1 = 1
backend_data_directory1 = &#39;/home1/testuser/psql/engn/PGSQL&#39;
backend_flag1 = &#39;ALLOW_TO_FAILOVER&#39;
...
#------------------------------------------------------------------------------
# FILE LOCATIONS
#------------------------------------------------------------------------------
pid_file_name = &#39;/var/run/pgpool.pid&#39;
logdir = &#39;/home1/testuser/psql/logs/$시스템명/&#39;
...
#------------------------------------------------------------------------------
# LOAD BALANCING MODE
#------------------------------------------------------------------------------
load_balance_mode = on
...
#------------------------------------------------------------------------------
# MASTER/SLAVE MODE
#------------------------------------------------------------------------------
master_slave_mode = on
master_slave_sub_mode = &#39;stream&#39;
...
# - Streaming -
sr_check_period = 5
sr_check_user = &#39;repl&#39;
sr_check_password = &#39;qhdks123&#39;

#------------------------------------------------------------------------------
# FAILOVER AND FAILBACK
#------------------------------------------------------------------------------

failover_command = &#39;/usr/pgsql-11/share/failover.sh %d %H /home1/testuser/psql/engn/PGSQL/failover_trigger&#39;
...
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pgpool Connection Settings : 외부에서 pgpool로 접속할 때 사용할 접속 정보&lt;/li&gt;
&lt;li&gt;Backend Connection Setting : Cluster에 속한 DB 정보 명시하는 부분, backend_weight : load-balancing 분산 비율 설정, backend_flag : failover 기능 사용 &amp;lsquo;ALLOW_TO_FAILOVER&#39;로 설정&lt;/li&gt;
&lt;li&gt;LOAD BALANCING MODE :  load balance 기능 사용 설정&lt;/li&gt;
&lt;li&gt;MASTER/SLAVE MODE : 현재 Streaming Replication을 사용하고 있기 때문에 master_slave_sub_mode는 &amp;lsquo;stream&#39;으로 설정&lt;/li&gt;
&lt;li&gt;Streaming : Streaming Replication 상태 체크를 위한 설정으로 5초마다 repl 계정으로 상태 Check 함&lt;/li&gt;
&lt;li&gt;failover_command : pgpool 에서 Cluster 내 DB 서버에 이상을 감지하면 실행하는 커맨드&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt; &#39;/usr/pgsql-9.6/share/failover.sh %d %H /var/lib/pgsql/9.6/data/failover_trigger&#39;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; %d : 이상현상이 발생한 node id , %H : new master가 될 node id
standby 서버에 failover_trigger 파일이 생기면 primary server 로 승격하겠다는 의미&lt;/p&gt;
&lt;h3 id=&#34;failoversh--&#34;&gt;failover.sh 스크립트 작성&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vi /usr/pgsql-11/share/failover.sh&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;if [ $# -ne 3 ]
then
        echo &amp;quot;failover failed_node new_master trigger_file&amp;quot;
        exit 1
fi
 
FAILED_NODE=$1
NEW_MASTER=$2
TRIGGER_FILE=$3
 
# Do nothing if standby server goes down
if [$FAILED_NODE = 1]; then
        echo &amp;quot;Standby Server is downed\n&amp;quot; &amp;gt;&amp;gt; /etc/pgpool-II/failover.log
        exit 0
fi
 
echo &amp;quot;failover.sh FAILED_NODE:${FAILED_NODE}; NEW_MASTER:${NEW_MASTER}; at $(date)\n&amp;quot; &amp;gt;&amp;gt; /etc/pgpool-II/failover.log
 
sudo -u testuser ssh -T testuser@$NEW_MASTER touch $TRIGGER_FILE
 
exit 0
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; Primay 서버에 이상이 생기면 Standby 서버에 failover trigger 파일을 생성하여 standby 서버가 승격될 수 있도록 함&lt;/p&gt;
&lt;h3 id=&#34;ssh-key-&#34;&gt;ssh key 설정&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;ssh-keygen -t rsa 수행 후
서로의 id_rsa.pub &amp;gt;&amp;gt; authorized_keys 작업
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; failover.sh 스크립트에서 ssh 를 통해 trigger file을 생성하기 때문에 pgpool 서버 &amp;lt;-&amp;gt; DB 서버 간 ssh 인증 설정필요&lt;/p&gt;
&lt;h3 id=&#34;pgpool-2&#34;&gt;pgpool 기동&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;pgpool 기동&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[testusersu@psql-server-1 15:38:26 /etc/pgpool-II ]$ sudo pgpool


[testuser@psql-server-1 15:40:42 ~/psql/logs/testdb ]$ ps -ef | grep pgpool
root     42462     1  0 15:38 ?        00:00:00 pgpool
root     42463 42462  0 15:38 ?        00:00:00 pgpool: wait for connection request
root     42464 42462  0 15:38 ?        00:00:00 pgpool: wait for connection request
.
.
.
root     42496 42462  0 15:38 ?        00:00:00 pgpool: PCP: wait for connection request
root     42497 42462  0 15:38 ?        00:00:00 pgpool: worker process
root     42498 42462  0 15:38 ?        00:00:00 pgpool: health check process(0)
root     42499 42462  0 15:38 ?        00:00:00 pgpool: health check process(1)
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;접속 테스트&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[testuser@psql-server-2 15:38:32 ~ ]$ psql -h &amp;lt;pgpool IP&amp;gt;  -p 4000 -d postgres
psql (11.7)
Type &amp;quot;help&amp;quot; for help.

postgres=# \x
Expanded display is on.

postgres=# show pool_nodes;
-[ RECORD 1 ]------+--------------------
node_id            | 0
hostname           | psql-server-1
port               | 3000
status             | up
lb_weight          | 0.500000
role               | primary
select_cnt         | 16
load_balance_node  | false
replication_delay  | 0
last_status_change | 2020-04-09 18:56:42
-[ RECORD 2 ]------+--------------------
node_id            | 1
hostname           | psql-server-2
port               | 3000
status             | up
lb_weight          | 0.500000
role               | standby
select_cnt         | 19
load_balance_node  | true
replication_delay  | 0
last_status_change | 2020-04-09 18:56:42

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; client 는 pgpool의 ip/port 로 접속하며 load_balancing 설정에 의해 1:1 로 쿼리 분산됨&lt;/p&gt;
&lt;h2 id=&#34;failover-test&#34;&gt;failover test&lt;/h2&gt;
&lt;h3 id=&#34;--&#34;&gt;현재 상태 체크&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# show pool_nodes;
-[ RECORD 1 ]------+--------------------
node_id            | 0
hostname           | psql-server-1
port               | 3000
status             | up
lb_weight          | 0.500000
role               | primary
select_cnt         | 0
load_balance_node  | true
replication_delay  | 0
last_status_change | 2020-04-10 10:11:15
-[ RECORD 2 ]------+--------------------
node_id            | 1
hostname           | psql-server-2
port               | 3000
status             | up
lb_weight          | 0.500000
role               | standby
select_cnt         | 0
load_balance_node  | false
replication_delay  | 0
last_status_change | 2020-04-10 10:11:15
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;primary-stop---&#34;&gt;Primary stop &amp;amp; 로그 확인&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;primary stop&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[testuser@psql-server-1 10:10:48 ~ ]$ stop
waiting for server to shut down.... done
server stopped
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;standby log&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### error_log 

2020-04-10 01:11:49 GMT LOG:  replication terminated by primary server
2020-04-10 01:11:49 GMT DETAIL:  End of WAL reached on timeline 1 at 0/7C000098.
2020-04-10 01:11:49 GMT FATAL:  could not send end-of-streaming message to primary: no COPY in progress
2020-04-10 01:11:49 GMT LOG:  invalid record length at 0/7C000098: wanted 24, got 0
2020-04-10 01:11:49 GMT FATAL:  could not connect to the primary server: server closed the connection unexpectedly
                This probably means the server terminated abnormally
                before or while processing the request.
2020-04-10 01:11:54 GMT LOG:  trigger file found: /home1/testuser/psql/engn/PGSQL/failover_trigger
2020-04-10 01:11:54 GMT LOG:  redo done at 0/7C000028
2020-04-10 01:11:54 GMT LOG:  last completed transaction was at log time 2020-04-10 10:11:49.045489+09
2020-04-10 01:11:54 GMT LOG:  selected new timeline ID: 2
2020-04-10 01:11:54 GMT LOG:  archive recovery complete
2020-04-10 01:11:54 GMT LOG:  database system is ready to accept connections

### read_only ON=&amp;gt;OFF 
postgres=# select name,setting from pg_settings where name like &#39;%read%&#39;;
             name              | setting
-------------------------------+---------
 default_transaction_read_only | off
 transaction_read_only         | off
(2 rows)

&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; primary 와 복제 끊긴 것 확인
pgpool failover command 에 의해 생성된 failover trigerr file 확인
recovery.conf failover_trigger 옵션에 의해 primary 로 승격시킴&lt;/p&gt;
&lt;h3 id=&#34;failover--&#34;&gt;failover 정상 체크&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# show pool_nodes;
-[ RECORD 1 ]------+--------------------
node_id            | 0
hostname           | psql-server-1
port               | 3000
status             | down
lb_weight          | 0.500000
role               | standby
select_cnt         | 0
load_balance_node  | false
replication_delay  | 0
last_status_change | 2020-04-10 10:11:54
-[ RECORD 2 ]------+--------------------
node_id            | 1
hostname           | psql-server-2
port               | 3000
status             | up
lb_weight          | 0.500000
role               | primary
select_cnt         | 2
load_balance_node  | true
replication_delay  | 0
last_status_change | 2020-04-10 10:11:54
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;failover--1&#34;&gt;failover 후 추가작업&lt;/h3&gt;
&lt;p&gt;failover 후 old master를 올려도 자동으로 slave 로 추가되는 기능은 제공되지 않기 때문에 아래와 같은 과정을 수동으로 진행한다&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;현재 상태&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# show pool_nodes;
 node_id |     hostname      | port | status | lb_weight |  role   | select_cnt | load_balance_node | replication_delay | last_status_c
hange
---------+-------------------+------+--------+-----------+---------+------------+-------------------+-------------------+--------------
-------
 0       | psql-server-1 | 3000 | down   | 0.500000  | standby | 0          | false             | 0                 | 2020-04-22 14
:28:21
 1       | psql-server-2 | 3000 | up     | 0.500000  | primary | 0          | true              | 0                 | 2020-04-22 14
:28:21
(2 rows)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; old master t1801 서버가 failover 되어 t1802 가 new master 가 된 상황
t1801 서버를 올려도 자동으로 slave로 추가되지 않으며 pgpool 은 이 서버를 인식하지 못하는 상태&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;old master slave로 재구성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;pg_basebackup -h new_master -p3000 -D /home1/testuser/psql/engn/PGSQL -U repl -P -v -X stream

vi recovery.conf

standby_mode = on
primary_conninfo=&#39;host=new_master port=3000 user=repl password=qhdks123&#39;
primary_slot_name=&#39;repl_slot_01&#39;
trigger_file=&#39;/home1/testuser/psql/engn/PGSQL/failover_trigger&#39;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;old master up&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;pg_ctl -D /home1/testuser/psql/engn/PGSQL start
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pgpool node로 추가&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[testuser@psql-server-1 14:53:40 ~/psql/logs/testdb/error_log ]$ pcp_attach_node -n 0
Password:
pcp_attach_node -- Command Successful


postgres=# show pool_nodes;
 node_id |     hostname      | port | status | lb_weight |  role   | select_cnt | load_balance_node | replication_delay | last_status_c
hange
---------+-------------------+------+--------+-----------+---------+------------+-------------------+-------------------+--------------
-------
 0       | psql-server-1 | 3000 | up     | 0.500000  | standby | 0          | true              | 0                 | 2020-04-22 14
:55:26
 1       | psql-server-2 | 3000 | up     | 0.500000  | primary | 6          | false             | 0                 | 2020-04-22 14
:28:21
(2 rows)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; old master 가 new master의 slave node 로 추가됨&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL Replication 설정-Streaming Replication</title>
      <link>https://kimdubi.github.io/postgresql/psql_replication/</link>
      <pubDate>Wed, 03 Jun 2020 17:49:11 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_replication/</guid>
      <description>&lt;h2 id=&#34;replication-&#34;&gt;REPLICATION 방식&lt;/h2&gt;
&lt;h3 id=&#34;log-shipping&#34;&gt;log shipping&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_repl1.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;file-based 복제로 master의 WAL file이 생성되면 이 파일을 scp를 통해 standby서버로 전달하여 반영함&lt;/li&gt;
&lt;li&gt;wal file이 생길 때 까지 replication gap 이 발생할 수 있음&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;logical-replication&#34;&gt;logical replication&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_repl2.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;pub / sub 구조로 양방향 replication 가능&lt;/li&gt;
&lt;li&gt;특정 테이블만 복제하는 partial replication 가능&lt;/li&gt;
&lt;li&gt;DDL 복제는 안되기 때문에 각각 수행해줘야함&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;streaming-replication&#34;&gt;streaming replication&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_repl3.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Master는 standyby 에게 transaction log entires 를 전달하고 standby 는 WAL file을 기다리지 않고 record 단위로 복제 수행&lt;/li&gt;
&lt;li&gt;일반적으로 가장 많이 사용되는 복제 방식&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;streaming-replication-&#34;&gt;streaming replication 설정&lt;/h2&gt;
&lt;h3 id=&#34;1-replication--user--master&#34;&gt;1. replication 용 user 생성 (master)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;postgres=# create user repl replication password &#39;qhdks123&#39;;
CREATE ROLE

postgres=# \du
                                   List of roles
 Role name |                         Attributes                         | Member of
-----------+------------------------------------------------------------+-----------
 kimdubi    | Superuser, Create role, Create DB, Replication, Bypass RLS | {}
 repl      | Replication                                                | {}
 testuser  | No inheritance                                            +| {}
           | Password valid until infinity                              |
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;2-postgresqlconf--master&#34;&gt;2. postgresql.conf 수정 (master)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vi /home1/kimdubi/psql/engn/PGSQL/postgresql.conf&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;wal_level = logical 또는 hot_standby
archive_mode = on
archive_command = &#39;cp %p /home1/kimdubi/psql/arch/testdb/%f&#39;
#max_wal_senders = 10
#max_replication_slots = 10 
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;쌓이는 archive logfile은 아래와 같이 정리 필요&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;pg_archivecleanup /home1/kimdubi/psql/arch/testdb 000000010000000000000081.00000028.backup
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt;  xxxx.backup 이전의 아카이브 파일 모두 삭제&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;postgresql.conf 수정 후 reload&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;pg_ctl reload -D /home1/kimdubi/psql/engn/PGSQL
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;3-pghbaconf--master&#34;&gt;3. pg_hba.conf 수정 (master)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vi /home1/kimdubi/psql/engn/PGSQL/postgresql.conf  아래 내용 추가&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;host    replication     repl             10.161.78.34/32            md5
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; standby server (10.161.78.34) 에서 repl USER로 replication을 위해 오는 커넥션 요청은 md5로 인증해서 허가함&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;pg_hba.conf 수정 후 reload&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;pg_ctl reload -D /home1/kimdubi/psql/engn/PGSQL
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;4-replication-slot--master&#34;&gt;4. replication slot 생성 (master)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;
postgres=# SELECT * FROM pg_create_physical_replication_slot(&#39;repl_slot_01&#39;);
  slot_name   | lsn
--------------+-----
 repl_slot_01 |
(1 row)

postgres=# select * from pg_replication_slots;
  slot_name   | plugin | slot_type | datoid | database | temporary | active | active_pid | xmin | catalog_xmin | restart_lsn | confirme
d_flush_lsn
--------------+--------+-----------+--------+----------+-----------+--------+------------+------+--------------+-------------+---------
------------
 repl_slot_01 |        | physical  |        |          | f         | f      |            |      |              |             |
(1 row)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; standby 서버가 replication을 따라가지 못하는 경우를 대비하여 WAL 로그를 replication_slot에 두고 보관하겠다는 의미&lt;br&gt;
replication_slot을 사용하면 wal_keep_segments 설정이 필요없음&lt;/p&gt;
&lt;h3 id=&#34;5-primary-db-server-copy-standby&#34;&gt;5. Primary DB server copy (standby)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;### 기존 data 영역 삭제 
[kimdubi@testserver2 15:37:09 ~/psql/engn ]$ rm -rf /home1/kimdubi/psql/engn/PGSQL

### Primary server =&amp;gt; standby server copy

[kimdubi@testserver2 15:40:06 ~/psql/engn ]$ pg_basebackup -h 11.111.11.11 -p3000 -D /home1/kimdubi/psql/engn/PGSQL -U repl -P -v -X stream
Password:


pg_basebackup: initiating base backup, waiting for checkpoint to complete
pg_basebackup: checkpoint completed
pg_basebackup: write-ahead log start point: 0/6000028 on timeline 1
pg_basebackup: starting background WAL receiver
pg_basebackup: created temporary replication slot &amp;quot;pg_basebackup_64119&amp;quot;
31584/31584 kB (100%), 5/5 tablespaces
pg_basebackup: write-ahead log end point: 0/60000F8
pg_basebackup: waiting for background process to finish streaming ...
pg_basebackup: base backup completed

&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;pg_basebackup -h primary_host -p primary_port -D PGDATA -U replication user&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;6-recoveryconf--standby&#34;&gt;6. recovery.conf 설정 (standby)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vi /home1/kimdubi/psql/engn/PGSQL/recovery.conf&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;standby_mode = on
primary_conninfo=&#39;host=11.111.11.11 port=3000 user=repl password=qhdks123&#39;
primary_slot_name=&#39;repl_slot_01&#39;
#trigger_file=&#39;/home1/kimdubi/psql/engn/PGSQL/failover_trigger&#39;
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;7-db--standby&#34;&gt;7. DB 기동 (standby)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver2 15:45:23 ~/psql/engn/PGSQL ]$ pg_ctl -D /home1/kimdubi/psql/engn/PGSQL start

waiting for server to start....2020-04-08 06:45:25 GMT LOG:  listening on IPv4 address &amp;quot;0.0.0.0&amp;quot;, port 3000
2020-04-08 06:45:25 GMT LOG:  listening on IPv6 address &amp;quot;::&amp;quot;, port 3000
2020-04-08 06:45:25 GMT LOG:  listening on Unix socket &amp;quot;/tmp/.s.PGSQL.3000&amp;quot;
2020-04-08 06:45:25 GMT LOG:  redirecting log output to logging collector process
2020-04-08 06:45:25 GMT HINT:  Future log output will appear in directory &amp;quot;/home1/kimdubi/psql/logs/testdb/error_log&amp;quot;.
 done
server started
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;8-replication-&#34;&gt;8. replication 확인&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;master&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;postgres=# \x
Expanded display is on.
postgres=# select * from pg_stat_replication;
-[ RECORD 1 ]----+-----------------------------
pid              | 65010
usesysid         | 16396
usename          | repl
application_name | walreceiver
client_addr      | 22.222.22.22
client_hostname  |
client_port      | 56754
backend_start    | 2020-04-08 16:05:07.00658+09
backend_xmin     |
state            | streaming
sent_lsn         | 0/B015A10
write_lsn        | 0/B015A10
flush_lsn        | 0/B015A10
replay_lsn       | 0/B015A10
write_lag        |
flush_lag        |
replay_lag       |
sync_priority    | 0
sync_state       | async

&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;slave&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### error_log 

2020-04-08 07:05:07 GMT LOG:  started streaming WAL from primary at 0/B000000 on timeline 1
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;replication delay 확인 (slave에서 조회)&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;SELECT CASE WHEN pg_last_wal_receive_lsn() = pg_last_wal_replay_lsn()
THEN 0
ELSE EXTRACT (EPOCH FROM now() - pg_last_xact_replay_timestamp())
END AS log_delay;


 log_delay
-----------
         0
(1 row)

&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;replication----&#34;&gt;replication 구성 중 발생가능 에러&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;standby =&amp;gt; primary 접근 권한 없을 때&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;2020-04-08 15:23:50.883 KST [44534] FATAL:  could not connect to the primary server: FATAL:  no pg_hba.conf entry for replication connection from host &amp;quot;22.222.22.22&amp;quot;, user &amp;quot;repl&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt;  primary server의 접근허용 리스트에 standby 서버 정보가 없는 경우 발생함
primary에서 pg_hba.conf 에 standby 서버 추가 후 reload&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;standby 서버 초기화 안했을 때&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;2020-04-08 15:26:51.084 KST [44642] FATAL:  database system identifier differs between the primary and standby
2020-04-08 15:26:51.084 KST [44642] DETAIL:  The primary&#39;s identifier is 6812776914427724112, the standby&#39;s identifier is 6813221629551992171.
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; standby 서버 $PGDATA 영역 삭제 후 다시 pg_basebackup 수행&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL configure 설정</title>
      <link>https://kimdubi.github.io/postgresql/psql_conf/</link>
      <pubDate>Tue, 02 Jun 2020 16:30:28 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_conf/</guid>
      <description>&lt;p&gt;yum 설치가 아닌 source make 설치라 디렉토리 구성 등이 다를 수 있습니다.&lt;/p&gt;
&lt;h3 id=&#34;bashprofile&#34;&gt;bash_profile&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ vi ~/.bash_profile

export POSTGRES_HOME=/home1/kimdubi/psql/engn/postgresql-11.7
export PGLIB=$POSTGRES_HOME/lib
export PGDATA=/home1/kimdubi/psql/engn/PGSQL
export MANPATH=$MANPATH:$POSTGRES_HOME/man

export PATH=$POSTGRES_HOME/bin:$PATH
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;postgresqlconf&#34;&gt;postgresql.conf&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;#------------------------------------------------------------------------------
# CONNECTIONS AND AUTHENTICATION
#------------------------------------------------------------------------------

# - Connection Settings -
listen_addresses = &#39;*&#39;
port = 3000
max_connections = 1000

#------------------------------------------------------------------------------
# RESOURCE USAGE (except WAL)
#------------------------------------------------------------------------------

# - Memory -
shared_buffers = 1024MB    
work_mem = 4MB   
maintenance_work_mem = 512MB 

dynamic_shared_memory_type = posix
effective_io_concurrency = 200

max_worker_processes=8
max_parallel_workers_per_gather = 2
max_parallel_workers = 8

#------------------------------------------------------------------------------
# WRITE AHEAD LOG
#------------------------------------------------------------------------------

# - Settings -
wal_level = logical
wal_buffers = 2MB
max_wal_size = 1GB

# - Archiving -
archive_mode = on
archive_command = &#39;cp %p /home1/kimdubi/psql/arch/testdb/%f&#39;

#------------------------------------------------------------------------------
# QUERY TUNING
#------------------------------------------------------------------------------

random_page_cost = 1.1
default_statistics_target = 100
effective_cache_size = 3GB   # max. Physical M * 0.75

#------------------------------------------------------------------------------
# ERROR REPORTING AND LOGGING
#------------------------------------------------------------------------------

# - Where to Log -
log_destination = &#39;stderr&#39;
logging_collector = on
log_directory = &#39;/home1/kimdubi/psql/logs/testdb/error_log&#39;
log_filename = &#39;alert_testdb.log&#39;

# - What to Log -
log_line_prefix = &#39;%t &#39;

#------------------------------------------------------------------------------
# CLIENT CONNECTION DEFAULTS
#------------------------------------------------------------------------------

# - Statement Behavior -
search_path = &#39;&amp;quot;$user&amp;quot;&#39;
temp_tablespaces = &#39;TS_TEMP&#39;

timezone=&#39;Asia/Seoul&#39;
lc_messages = &#39;en_US.UTF-8&#39; 
lc_monetary = &#39;en_US.UTF-8&#39; 
lc_numeric = &#39;en_US.UTF-8&#39; 
lc_time = &#39;en_US.UTF-8&#39; 
default_text_search_config = &#39;pg_catalog.english&#39;

&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;shared_buffer : data buffer , 전체 서버 메모리의 1/4 ~ 1/3 권장&lt;/li&gt;
&lt;li&gt;work_mem : sort,hash join 등 query 수행 시 사용하는 session memory, tempfile 생성이 많으면 더 크게 설정 필요&lt;/li&gt;
&lt;li&gt;maintenance_work_mem : vacuum, create index 에 사용되는 메모리, 전체 서버 메모리의 1/16 ~ 2GB(max)&lt;/li&gt;
&lt;li&gt;max_worker_processes : scan, group by, join 작업에 병렬처리를 지원함. parallel 처리를 수행하는 worker 프로세스의 최대 개수&lt;/li&gt;
&lt;li&gt;max_parallel_workers_per_gather : 하나의 쿼리 당 수행 가능한 최대 worker 프로세스 개수 (defalut 0 - parallel 처리 수행 안함)&lt;/li&gt;
&lt;li&gt;wal_level : WAL 파일에 기록되는 정보의 수준 설정 , mysql 로 비교하면 logical - ROW, replica - MIXED&lt;/li&gt;
&lt;li&gt;wal_buffers : 트랜잭션 로그(WAL log) 버퍼 , shared_buffers 의 1/32 권장&lt;/li&gt;
&lt;li&gt;archive_command : WAL file을 아카이브할 때 사용할 커맨드, %p 는 WAL 로그파일의 절대경로 %f 는 보관할 로그파일 이름&lt;/li&gt;
&lt;li&gt;random_page_cost : random I/O cost 추정값으로 높으면 풀스캔 선호, 상대적으로 줄이면 인덱스 스캔을 선호함 default 4.0&lt;/li&gt;
&lt;li&gt;effective_cache_size : query planner 에게 가용한 caching data의 크기를 알려줘서 cost 예측에 도움을 주는 설정.
값이 높다고 그만큼 memory를 점유하진 않고 index scan을 할 가능성이 높아짐, 전체 메모리의 3/4 권장 default 4GB&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;pghbaconf&#34;&gt;pg_hba.conf&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;# TYPE  DATABASE        USER            ADDRESS                 METHOD

# &amp;quot;local&amp;quot; is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
# IPv6 local connections:
host    all             all             ::1/128                 trust
# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            trust
host    replication     all             ::1/128                 trust
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; postgresql 접속 인증 관련 설정으로 oracle 의 sqlnet.ora , cubrid의 broker.access &amp;amp; iplist 파일, mysql의 user@host 와 같은 개념&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;예시 1) local 에서 모든 유저가 패스워드없이 (trust) 모든 데이터베이스에 접속 허용&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;예시 2) 111.111.11.x 대역의 모든 호스트는 testdb DB에 testuser 계정으로 md5 인증방식으로만 접속 허용&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;# TYPE  DATABASE        USER            ADDRESS                 METHOD
  host   testdb         testuser        111.111.11.0/24           md5
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;-&#34;&gt;참고 사이트&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.postgresql.org/docs/11/index.html&#34;&gt;https://www.postgresql.org/docs/11/index.html&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://postgresql.kr/blog/simple_postgresql_conf.html&#34;&gt;https://postgresql.kr/blog/simple_postgresql_conf.html&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
    <item>
      <title>PostgreSQL source compile install</title>
      <link>https://kimdubi.github.io/postgresql/psql_install/</link>
      <pubDate>Tue, 02 Jun 2020 16:23:46 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_install/</guid>
      <description>&lt;p&gt;간단하게 yum install 할 수도 있지만&lt;br&gt;
디렉토리 구성 등을 기본 default 설정과는 다르게 구성하여 관리하고 싶어
source 파일을 다운받아 컴파일 설치하는 식으로 구성해봤습니다.&lt;br&gt;
때문에 여기서의 디렉토리는 default 설정이 아닌 제 임의로 설정한 구성입니다.&lt;/p&gt;
&lt;h2 id=&#34;postgresql--&#34;&gt;postgresql 버전 확인&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://www.postgresql.org/ftp/source/&#34;&gt;https://www.postgresql.org/ftp/source/&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;heading&#34;&gt;설치&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;directory 생성 (kimdubi 계정)&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;mkdir -p /home1/kimdubi/psql/engn/    =&amp;gt; PostgreSQL engine 영역
mkdir -p /home1/kimdubi/psql/data/testdb  =&amp;gt; tablespace 등data 저장공간
mkdir -p /home1/kimdubi/psql/logs/testdb/error_log  =&amp;gt; error log 
mkdir -p /home1/kimdubi/psql/arch/testdb  =&amp;gt; pg_wal 아카이빙하는 공간
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;11.7 버전 download &amp;amp; 압축해제&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver 15:34:53 ~/psql/engn ]$ pwd
/home1/kimdubi/psql/engn

wget https://ftp.postgresql.org/pub/source/v11.7/postgresql-11.7.tar.gz
tar zxvpf postgresql-11.7.tar.gz
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;make install&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver 20:09:06 ~/psql/engn/postgresql-11.7 ]$ pwd
/home1/kimdubi/psql/engn/postgresql-11.7


./configure --prefix=/home1/kimdubi/psql/engn/postgresql-11.7/ --enable-depend --enable-nls=utf8 --with-python
make
make install
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;DB 설치&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver 20:07:47 ~/psql/engn/postgresql-11.7/bin ]$ pwd
/home1/kimdubi/psql/engn/postgresql-11.7/bin

### initdb 커맨드 수행 -D datadir
 ./initdb -E utf8 -D /home1/kimdubi/psql/engn/PGSQL/ 
 
 
 ### 설치 수행 로그

The files belonging to this database system will be owned by user &amp;quot;kimdubi&amp;quot;.
This user must also own the server process.

The database cluster will be initialized with locale &amp;quot;C&amp;quot;.
The default text search configuration will be set to &amp;quot;english&amp;quot;.

Data page checksums are disabled.

fixing permissions on existing directory /home1/kimdubi/psql/engn/PGSQL ... ok
creating subdirectories ... ok
selecting default max_connections ... 100
selecting default shared_buffers ... 128MB
selecting default timezone ... ROK
selecting dynamic shared memory implementation ... posix
creating configuration files ... ok
running bootstrap script ... ok
performing post-bootstrap initialization ... ok
syncing data to disk ... ok

WARNING: enabling &amp;quot;trust&amp;quot; authentication for local connections
You can change this by editing pg_hba.conf or using the option -A, or
--auth-local and --auth-host, the next time you run initdb.

Success. You can now start the database server using:

    ./pg_ctl -D /home1/kimdubi/psql/engn/PGSQL/ -l logfile start


&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;설치 확인 &amp;amp; 파라미터 수정&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver 10:39:21 ~/psql/engn/PGSQL ]$ pwd
/home1/kimdubi/psql/engn/PGSQL

[kimdubi@testserver 10:39:33 ~/psql/engn/PGSQL ]$ ls
PG_VERSION  pg_commit_ts  pg_ident.conf  pg_notify    pg_snapshots  pg_subtrans  pg_wal                postgresql.conf
base        pg_dynshmem   pg_logical     pg_replslot  pg_stat       pg_tblspc    pg_xact
global      pg_hba.conf   pg_multixact   pg_serial    pg_stat_tmp   pg_twophase  postgresql.auto.conf


vi postgresql.conf

vi ~./bash_profile

vi pg_hba.conf
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;=&amp;gt; oracle 설치와 비슷하게 엔진 설치 후 parameter 파일 설정 &amp;amp; DB 기동하는 순서&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;DB 기동&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;[kimdubi@testserver 10:49:59 ~/psql/engn/postgresql-11.7/bin ]$ pg_ctl -D /home1/kimdubi/psql/engn/PGSQL/ start
waiting for server to start....2020-04-07 01:50:00 GMT LOG:  listening on IPv4 address &amp;quot;0.0.0.0&amp;quot;, port 3000
2020-04-07 01:50:00 GMT LOG:  listening on IPv6 address &amp;quot;::&amp;quot;, port 3000
2020-04-07 01:50:00 GMT LOG:  listening on Unix socket &amp;quot;/tmp/.s.PGSQL.3000&amp;quot;
2020-04-07 01:50:00 GMT LOG:  redirecting log output to logging collector process
2020-04-07 01:50:00 GMT HINT:  Future log output will appear in directory &amp;quot;/home1/kimdubi/psql/logs/testdb/error_log&amp;quot;.
 done
server started

[kimdubi@testserver 10:50:26 ~/psql/engn/postgresql-11.7/bin ]$ pg_ctl -D /home1/kimdubi/psql/engn/PGSQL/ status
pg_ctl: server is running (PID: 13079)
/home1/kimdubi/psql/engn/postgresql-11.7/bin/postgres &amp;quot;-D&amp;quot; &amp;quot;/home1/kimdubi/psql/engn/PGSQL&amp;quot;

&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;--&#34;&gt;생성 후 작업&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;tablespace / database 생성&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### TABLESPACE 생성
-. OS 디렉토리 생성
mkdir -p /home1/kimdubi/psql/data/testdb/TS_USER
mkdir -p /home1/kimdubi/psql/data/testdb/TS_TEMP
mkdir -p /home1/kimdubi/psql/data/testdb/TS_TEST_D
mkdir -p /home1/kimdubi/psql/indx/testdb/TS_TEST_I

-. DB tablespace 생성
postgres=# create tablespace TS_USER location &#39;/home1/kimdubi/psql/data/testdb/TS_USER&#39;;
postgres=# create tablespace TS_TEMP location &#39;/home1/kimdubi/psql/data/testdb/TS_TEMP&#39;;
postgres=# create tablespace TS_TEST_D location &#39;/home1/kimdubi/psql/data/testdb/TS_TEST_D&#39;;
postgres=# create tablespace TS_TEST_I location &#39;/home1/kimdubi/psql/indx/testdb/TS_TEST_I&#39;;
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;USER 생성
=&amp;gt; 복잡하지만 서비스용 schema, user를 별도로 생성했습니다.&lt;br&gt;
보안 및 권한으로 인한 장애를 막기 위함인데요&lt;br&gt;
PostgreSQL은 ORACLE / MySQL 과 user,role,schema 개념이 조금 달라 헷갈릴 수 있습니다.
이 부분은 다음 시간에 별도로 다루겠습니다.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### 서비스 계정

create role testuser login password &#39;!dlatl00&#39; noinherit valid until &#39;infinity&#39;;
create schema testuser authorization testuser;
alter role testuser set default_tablespace=ts_test_d;
grant create on tablespace ts_test_d to testuser;
grant create on tablespace ts_test_i to testuser;
grant create on tablespace ts_temp to testuser;

###
postgres=# create database testdb  tablespace=&#39;ts_test_d&#39; with owner kimdubi;
CREATE DATABASE
&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;
&lt;li&gt;권한 설정&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;### super user로 db 접속
testdb=&amp;gt; \c testdb kimdubi

### 다른 계정의 접근 차단 및 public schema 사용 권한 회수
testdb=# revoke all on database testdb from public;
testdb=# revoke all on schema public from public;

### 서비스계정에 권한 추가
testdb=# create schema testuser authorization testuser;

grant connect,temporary on database testdb to testuser;
grant usage on schema testuser to testuser;
grant select,insert,update,delete on all tables in schema testuser to testuser;
grant usage on all sequences in schema testuser to testuser;
alter default privileges in schema testuser grant select,insert,update,delete on tables to testuser;
alter default privileges in schema testuser grant usage on sequences to testuser;

### 새로운 계정 추가되는 경우
create role new_testuser login password &#39;!dlatl00&#39; noinherit valid until &#39;infinity&#39;;
grant connect,temporary on database testdb to new_testuser;
grant usage on schema testuser to new_testuser;
grant select,insert,update,delete on all tables in schema testuser to new_testuser;

DB 접속 권한 확인 =&amp;gt; schema usage 권한 확인 =&amp;gt; 각 개별 테이블 권한 확인
&lt;/code&gt;&lt;/pre&gt;</description>
    </item>
    
    <item>
      <title>PostgreSQL Architecture (PostgreSQL 시작하기)</title>
      <link>https://kimdubi.github.io/postgresql/psql_architecture/</link>
      <pubDate>Tue, 02 Jun 2020 15:50:45 +0900</pubDate>
      
      <guid>https://kimdubi.github.io/postgresql/psql_architecture/</guid>
      <description>&lt;p&gt;postgresql은 프로세스 기반의 GIS 기능으로 유명한 오픈소스DB 입니다.&lt;br&gt;
세계적인 인기(?) 에 비해 유독 우리나라에선 postgresql 을 사용하는 곳이 드문데&lt;br&gt;
저도 아무것도 모르지만 설치부터 하나하나 공부해가는 마음으로 포스팅을 시작하겠습니다.&lt;br&gt;
제가 구성하고자 하는 PostgreSQL 아키텍처는 아래와 같습니다.&lt;/p&gt;
&lt;h2 id=&#34;-&#34;&gt;셋업 환경&lt;/h2&gt;
&lt;h3 id=&#34;os--db&#34;&gt;OS &amp;amp; DB&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;OS : CentOS release 6.9&lt;/li&gt;
&lt;li&gt;DB : PostgreSQL 11.7 (20.02 기준 postgresql 12 버전 출시되었으나 안정화된 11버전 설치)
&lt;ul&gt;
&lt;li&gt;replication : streaming-replication (master-standby)&lt;/li&gt;
&lt;li&gt;auto-failover : pgpool-II&lt;/li&gt;
&lt;li&gt;backup : pg_start_backup&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;아래는 위 아키텍처 요소들에 대한 간단한 설명입니다.&lt;/p&gt;
&lt;h2 id=&#34;postgresql-&#34;&gt; PostgreSQL 아키텍처&lt;/h2&gt;
&lt;h3 id=&#34;single&#34;&gt;single&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_architecture1.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;프로세스 기반 dbms로 oracle 과 전체적인 아키텍처가 유사함&lt;/li&gt;
&lt;li&gt;1개의 connection 마다 1개의 background process 생성됨&lt;/li&gt;
&lt;li&gt;postmaster : postgresql 기동 시 가장 먼저 시작되는 프로세스. 다른 background process를 기동하고 user와의 connection 생성하는 기능을 수행&lt;/li&gt;
&lt;li&gt;shared buffer : 데이터 캐싱하는 메모리, 서버 메모리의 1/4 이상 권장됨&lt;/li&gt;
&lt;li&gt;WAL buffer : 세션들이 수행하는 트랜잭션에 대한 변경 로그를 캐싱함 (redo log) shared buffer 의 1/32 이상 권장됨&lt;/li&gt;
&lt;li&gt;CLOG buffer : commit log, 트랜잭션 상태를 캐싱하는 메모리 공간으로 트랜잭션이 commit 되었는지를 기록함&lt;/li&gt;
&lt;li&gt;autovacuum : mvcc를 위해 update /delete 수행 시 db에 다중 버전의 레코드를 저장하는데 vacuum 을 통해 더이상 참조되지 않는 레코드를 정리함, 그 외에도 트랜잭션 ID (XID) 정리 , 테이블 통계작업 등을 수행함&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;replication-streamingreplication&#34;&gt;replication (streaming-replication)&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_architecture2.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Master 에서 생성된 WAL log를 Slave DB로 실시간성으로 전달하여 정합성을 일치시키는 replication 방법&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;ha-with-auto-failover--pgpool-2&#34;&gt;HA with auto failover ( pgpool 2)&lt;/h3&gt;
&lt;p&gt;&lt;img src=&#34;https://raw.githubusercontent.com/kimdubi/kimdubi.github.io/master/images/psql/psql_architecture3.png&#34; alt=&#34;&#34;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;pgpool 미들웨어를 앞단에 두어 load balancing , auto failover 기능을 수행함&lt;/li&gt;
&lt;/ul&gt;
</description>
    </item>
    
  </channel>
</rss>
